# 计划改动

> 基于代码审查和讨论确认的最终改动方案。

---

## 严重Bug（优先修）

### 1. WebSocket 事件名不匹配 — 实时推送完全失效

**文件：** `server/src/jobs/hotspotChecker.ts` + `client/src/pages/HotspotsPage.tsx`

**现状：** 服务端发射 `'hotspot:new'` 和 `'notification'`，前端监听 `'newHotspot'` 和 `'newNotification'`。事件名不对，前端永远收不到实时推送。

**改法：** 服务端改为 `'newHotspot'` 和 `'newNotification'`，与前端一致。

### 2. 前端筛选值大小写不匹配数据库

**文件：** `client/src/components/FilterBar.tsx` + `server/src/routes/hotspots.ts`

**现状：** `FilterBar.tsx` 选项值使用大写（`SEC_EDGAR` / `CAILIAN` / `HIGH` / `MEDIUM` / `LOW`），DB 存储为小写（`sec_edgar` / `cailianshe` / `high` / `medium` / `low`）。筛选传过去匹配不上。

**改法：**
- 前端信源下拉：`SEC_EDGAR` → `sec_edgar`，`JUCHAO` → `juchao`，`CAILIAN` → `cailianshe`，`EASTMONEY` → `eastmoney`，`FRED` → `fred`，`NBS` → `nbs`
- 前端重要性下拉：`HIGH` → `high`，`MEDIUM` → `medium`，`LOW` → `low`
- 后端 `hotspots.ts` 加一层 `toLowerCase()` 归一化作为防御

### 3. FRED API Key 缺失时硬崩溃

**文件：** `scripts/sources/fred.py`

**现状：** FRED API Key 为空时 `raise ValueError`，导致整轮宏观 cron 崩溃。其他源缺 key 只是跳过。

**改法：** `raise ValueError` → `return [], watermark`（返回空列表）

---

## 阶段A：规划期

### 4. 快车道改为子串匹配 + 加搜快讯

**文件：** `server/src/jobs/hotspotChecker.ts` + `server/src/config/stockCodes.ts`

**现状：** `A_STOCK_CODE_MAP` 精确 key 匹配，`"寒武纪最近表现"` 匹配不上。命中后只搜巨潮，遗漏快讯。

**改法：**
- `A_STOCK_CODE_MAP[keyword.text]` 精确匹配 → 遍历 key 做子串匹配（`keyword.text.includes(companyName)`）
- `stockCodes.ts` 加别名（如 `"茅台":"600519"`、`"万科":"000002"`）
- 命中后 `targetSources` 从 `['juchao']` 改为 `['juchao', 'cailianshe', 'eastmoney']`

### 5. 策略缓存键改为核心实体标识

**文件：** `server/prisma/schema.prisma` + `server/src/jobs/hotspotChecker.ts` + `server/src/services/ai.ts`

**现状：** 缓存以 `keyword.text` 为键，`"美国CPI"` 和 `"美国如今CPI数据"` 是两条记录，互不相通。

**改法：**
- Keyword 表新增 `normalizedKey` 字段
- 新增 `extractCoreEntity(keyword)` 函数，利用已有的 `detectKeywordType()`：

| 关键词类型 | 提取方式 | 缓存键示例 |
|-----------|---------|-----------|
| `stock_code` | 直接用代码 | `stock:000002` |
| `stock_name` | 查映射表得代码 | `stock:688256` |
| `macro_indicator` | 正则提取指标名 | `macro:CPI` |
| `sector` | 精确匹配板块名 | `sector:半导体` |
| `policy` | 提取政策关键词 | `policy:房地产` |
| `generic` | AI生成后缓存 | `generic:AI` |

- 查缓存逻辑：先算 `normalizedKey`，用它在DB里匹配已有策略

### 6. 关键词跳过不匹配的cron路线（前置化）

**文件：** `server/src/jobs/hotspotChecker.ts`

**现状：** 代码已有 `strategySources` 取交集来跳过不匹配的 cron（第134-145行），交集为空时直接 `continue` 不调 Python。但跳过发生在策略解析之后——非快车道关键词仍需从 DB 读缓存或调 AI 才能出策略。且跳过逻辑夹在流程中间，不够清晰。

**改法：** 关键词循环入口处（第90行附近）提前用 `normalizedKey` 拿缓存策略，先判断与本次 cron 的信源是否有交集，无交集则直接 `continue`，不触发后续的策略解析和关键词扩展。

---

## 阶段B：采集期

### 7. 快讯源同源共享拉取（50条/次）

**文件：** `server/src/jobs/hotspotChecker.ts`

**现状：** 关键词串行循环，每个关键词对快讯源独立调一次 collector.py。"CPI"和"GDP"同时监控时，财联社被拉两次各50条。

**改法：** 关键词循环前，对快讯源先拉一次数据并缓存，循环内复用缓存结果做预匹配。每个源50条不变——水位线会自动过滤已见过的，配额控制会限制进AI的数量。

### 8. 宏观cron频率调整

**文件：** `server/src/index.ts`

**现状：** 宏观源每 2 小时扫描一次，CPI 一个月才变一次，绝大多数是空扫。

**改法：** `0 */2 * * *` → `0 5 * * *`（每天早5点一次）

### 9. collector.py 加重试机制

**文件：** `server/src/services/collector.ts`

**现状：** API 超时或返回错误直接抛异常，整轮 cron 失败。

**改法：** `collectFromSource()` 加指数退避重试（最多3次），超过阈值才报错。

---

## 阶段C：初筛期

### 10. 快讯预匹配增加默认变体

**文件：** `server/src/services/ai.ts`

**现状：** 纯文本子串匹配，"AI" 和 "人工智能" 匹配不上。完全依赖关键词扩展的 AI 质量。

**改法：** 在 `detectKeywordType()` 基础上为每种类型预置默认变体——`stock_code` 自动附加公司名、`macro_indicator` 自动附加中英文名。

### 11. 巨潮黑名单"关于公司"细化

**文件：** `scripts/sources/juchao.py`

**现状：** `"关于公司"` 太宽泛，可能误杀有用公告。代码里自己注释了"先保留"。

**改法：** 改为更长的完整模式（如 `"关于公司股份"`、`"关于公司名称"`），减少误杀。

---

## 阶段D：精筛期

### 12. `detectKeywordType()` 接入 filter.ts，按类型分阈值

**文件：** `server/src/utils/filter.ts` + `server/src/jobs/hotspotChecker.ts`

**现状：** `detectKeywordType()` 已写好6种类型检测，但 `shouldFilter()` 完全未使用。所有类型统一用三条硬编码规则。

**改法：** `shouldFilter()` 新增 `keywordType` 参数，不同类型走不同阈值：

| 类型 | 非实质+rel | 纯rel | 未提及+rel |
|------|-----------|-------|-----------|
| stock_code / stock_name | <20 丢 | <25 丢 | <40 丢 |
| sector | <15 丢 | <20 丢 | <25 丢（放宽） |
| macro_indicator | <15 丢 | <20 丢 | <25 丢（放宽） |
| policy | <15 丢 | <20 丢 | <25 丢（放宽） |
| generic | <20 丢 | <25 丢 | <40 丢 |

板块和宏观类放宽"未提及关键词"规则——政策新闻可能不直接出现关键词名称但影响重大。

### 13. AI fallback 默认分数提高

**文件：** `server/src/services/ai.ts`

**现状：** 无 API Key 时，提及→40、未提及→10。AI调用失败时，提及→30、未提及→10。分数太低，大概率被阈值过滤丢掉。

**改法：**
- 无 API Key：提及→**60**，未提及→**30**
- AI 调用失败：提及→**50**，未提及→**25**

### 14. 配额按关键词类型分档

**文件：** `server/src/jobs/hotspotChecker.ts`

**现状：** `MAX_PER_SOURCE=10`、`MAX_TOTAL=50`，所有关键词一视同仁。

**改法：**

| 类型 | MAX_PER_SOURCE | MAX_TOTAL |
|------|---------------|-----------|
| stock_code / stock_name | 15 | 80 |
| sector | 10 | 50 |
| macro_indicator | 3 | 10 |
| policy | 10 | 50 |
| generic | 10 | 50 |

### 15. 跨源去重窗口延长

**文件：** `server/src/jobs/hotspotChecker.ts`

**现状：** `eventFingerprint` 检查只查 30 分钟内的记录。财报发布等重大事件可能数小时内被多源报道。

**改法：** 30 分钟 → **6 小时**

---

## 阶段E：产出期

### 16. 数据保留期按重要性分级

**文件：** `server/src/index.ts`

**现状：** 每天凌晨 3 点删除 3 天前的所有数据。

**改法：**
- high / medium：保留 **7 天**
- low：保留 **3 天**

---

## 全局层面

### 17. 关键词串行改并发

**文件：** `server/src/jobs/hotspotChecker.ts`

**现状：** `for (const keyword of keywords)` 串行，10个关键词30秒/个 = 5分钟。后面等着前面。

**改法：** 改为并发执行，`Promise.all()` + 并发池（同时跑3个），注意控制并发数避免 API 限频和 DB 写入冲突。
