# 金融热点监控系统 — 第一阶段开发指南

> 基于原始方案 + 代码审查修订 + 遗漏补充，具备强指导性的完整开发文档。
> 后续实施将严格按照本指南的批次顺序执行。

---

## 一、修订摘要（相对原方案的变更）

| 原条目 | 修订内容 | 原因 |
|--------|---------|------|
| 第 5 条 | 修正描述：不是"以 keyword.text 为 key 的独立缓存"，而是"不同 Keyword 记录各自独立存储 searchStrategy，无法跨关键词共享" | 代码实际是每条 Keyword 记录存 searchStrategy 字段，不是独立缓存 Map |
| 第 8 条 | `0 5 * * *` → `0 21 * * *`（北京时间 21:00） | FRED 重要数据（CPI、非农）美东 8:30 发布 = 北京 20:30，5 点跑一次会延迟 20h+ |
| 第 15 条 | 30min → **3 小时**（而非 6 小时） | 6h 窗口过大，AI 生成的 fingerprint 不完全可靠，误去重风险高；3h 覆盖多源报道的常见时差 |
| 新增 Bug4 | 后端 `isSubstantial` 筛选参数被忽略 | 前端发了但后端没接收，筛选无效 |
| 新增 Bug5 | 通知面板颜色分类永远不生效 | 后端固定 type:'hotspot'，前端判断 HIGH_RELEVANCE/SUBSTANTIAL_EVENT 永远不匹配 |
| 新增 A0 | Keyword.type 字段未自动填充 | detectKeywordType() 已写好但从未调用，所有关键词 type 都是 "generic"，后续分阈值/分档全部失效 |
| 新增 A1 | Prisma 迁移步骤 | 第 5 条加 normalizedKey 字段需要 DB 迁移 |
| 新增 A2 | 并发改造的 DB 写入冲突防护 | 第 17 条并发后 eventFingerprint 去重可能竞态 |
| 新增 A3 | ensureStockCodes 与快车道子串匹配逻辑统一 | 两处逻辑重叠，需统一为同一函数 |

---

## 二、修改批次与执行顺序

依赖关系决定了必须按以下 5 个批次顺序执行。**同一批次内的条目无依赖关系，可并行实施。**

```
批次0（Bug 修复）  ← 无依赖，最先修
  │
批次1（基础设施）  ← 依赖批次0的 Bug4/A0
  │
批次2（采集优化）  ← 依赖批次1的 normalizedKey
  │
批次3（筛选优化）  ← 依赖批次1的 A0（keywordType 可用）
  │
批次4（产出+全局） ← 依赖批次3的筛选逻辑稳定
```

---

## 批次 0：严重 Bug 修复（5 项）

> **目标**：修复所有导致功能完全失效或数据错误的 Bug。
> **验证标准**：每个 Bug 修完后可独立验证，不依赖其他修改。

---

### Bug1. WebSocket 事件名不匹配

**优先级**：P0 — 实时推送完全失效

**涉及文件**：
- `server/src/jobs/hotspotChecker.ts` 第 333-334 行

**现状**：
```typescript
io.emit('hotspot:new', hotspot);
io.emit('notification', { ... });
```
前端监听 `newHotspot` / `newNotification`，事件名对不上。

**改法**：
```typescript
io.emit('newHotspot', hotspot);
io.emit('newNotification', { ... });
```

**验证**：启动服务后在前端浏览器 DevTools Network → WS 标签，确认收到 `newHotspot` 事件。

---

### Bug2. 前端筛选值大小写不匹配

**优先级**：P0 — 信源和重要性筛选完全无效

**涉及文件**：
- `client/src/components/FilterBar.tsx` 第 9-16 行（信源选项值）+ 第 87-90 行（重要性选项值）
- `server/src/routes/hotspots.ts` 第 10-19 行（后端 query 解构）

**改法**：

1. FilterBar.tsx 信源选项值改为 DB 实际存储值：
```typescript
const sources = [
  { value: 'sec_edgar', label: 'SEC EDGAR' },
  { value: 'juchao', label: '巨潮公告' },
  { value: 'cailianshe', label: '财联社' },
  { value: 'eastmoney', label: '东财全球' },
  { value: 'fred', label: 'FRED' },
  { value: 'nbs', label: '国家统计局' },
];
```

2. FilterBar.tsx 重要性选项值改为小写：
```typescript
<option value="high">高</option>
<option value="medium">中</option>
<option value="low">低</option>
```

3. hotspots.ts 后端加防御性归一化（在 query 解构后）：
```typescript
if (source) where.source = (source as string).toLowerCase();
if (importance) where.importance = (importance as string).toLowerCase();
```

**验证**：前端选择"财联社"筛选，确认返回 source=cailianshe 的热点。

---

### Bug3. FRED API Key 缺失硬崩溃

**优先级**：P0 — 整轮宏观 cron 崩溃

**涉及文件**：
- `scripts/sources/fred.py` 第 72-73 行

**现状**：
```python
if not API_KEY:
    raise ValueError("FRED_API_KEY not set in environment")
```

**改法**：
```python
if not API_KEY:
    print("  FRED: API key not set, skipping", file=sys.stderr)
    return [], watermark
```

**验证**：不设 FRED_API_KEY，运行宏观 cron，确认不崩溃且日志输出 skipping。

---

### Bug4. 后端 isSubstantial 筛选参数被忽略（新增）

**优先级**：P0 — "事件性质"筛选无效

**涉及文件**：
- `server/src/routes/hotspots.ts` 第 10-19 行

**现状**：前端发送 `isSubstantial=true/false`，后端 query 解构里没有这个字段。

**改法**：在 query 解构中添加：
```typescript
const {
  page = '1',
  limit = '20',
  source,
  importance,
  keywordId,
  sourceType,
  isSubstantial,  // 新增
  sortBy = 'createdAt',
  sortOrder = 'desc',
} = req.query;

// where 条件中添加：
if (isSubstantial !== undefined) {
  where.isSubstantial = isSubstantial === 'true';
}
```

**验证**：前端选择"实质性事件"筛选，确认只返回 isSubstantial=true 的热点。

---

### Bug5. 通知面板颜色分类不生效（新增）

**优先级**：P1 — 功能不致命但体验差

**涉及文件**：
- `server/src/jobs/hotspotChecker.ts` 第 314-321 行（创建通知处）
- `client/src/pages/HotspotsPage.tsx` 第 306-308 行（颜色判断）

**现状**：后端固定 `type: 'hotspot'`，前端判断 `HIGH_RELEVANCE` / `SUBSTANTIAL_EVENT` 永远不匹配。

**改法**：

1. 后端根据重要性设置通知类型：
```typescript
await prisma.notification.create({
  data: {
    type: analysis.importance === 'high' ? 'HIGH_RELEVANCE'
        : analysis.isSubstantial ? 'SUBSTANTIAL_EVENT'
        : 'hotspot',
    title: `新热点: ${hotspot.title.slice(0, 50)}`,
    content: analysis.summary || hotspot.content.slice(0, 100),
    hotspotId: hotspot.id,
  },
});
```

2. 前端 Socket 推送也带上 type：
```typescript
io.emit('newNotification', {
  type: analysis.importance === 'high' ? 'HIGH_RELEVANCE'
      : analysis.isSubstantial ? 'SUBSTANTIAL_EVENT'
      : 'hotspot',
  title: '发现新热点',
  content: hotspot.title,
  hotspotId: hotspot.id,
  importance: hotspot.importance,
});
```

**验证**：产生 high 重要性热点后，通知面板圆点显示为红色（bg-high）。

---

## 批次 1：基础设施（4 项）

> **目标**：建立后续优化所需的基础能力（keywordType 自动填充、normalizedKey、Prisma 迁移、逻辑统一）。
> **前置依赖**：批次 0 完成。

---

### A0. Keyword.type 自动填充（新增）

**优先级**：P0 — 后续第 12、14 条完全依赖此功能

**涉及文件**：
- `server/src/routes/keywords.ts`（创建/更新关键词处）
- `server/src/services/ai.ts`（导出 detectKeywordType）

**改法**：

1. ai.ts 中导出 detectKeywordType：
```typescript
export function detectKeywordType(keyword: string): KeywordType {
  // 现有函数体不变，只需加 export
}
```

2. keywords.ts 创建关键词时自动设置 type：
```typescript
import { detectKeywordType } from '../services/ai.js';

// POST 创建关键词
router.post('/', async (req, res) => {
  const { text } = req.body;
  const type = detectKeywordType(text);
  // ... 创建时传入 type
});
```

3. 对现有 DB 数据做一次性迁移（见 A1 迁移脚本中一并处理）。

**验证**：新建关键词"CPI"，确认 DB 中 type 为 "macro_indicator"。

---

### A1. Prisma 迁移：新增 normalizedKey 字段（新增）

**优先级**：P0 — 第 5、6 条依赖此字段

**涉及文件**：
- `server/prisma/schema.prisma`

**改法**：

1. schema.prisma Keyword 模型新增字段：
```prisma
model Keyword {
  // ... 现有字段
  normalizedKey String?   // 核心实体标识，如 stock:688256, macro:CPI
}
```

2. 添加索引（用于跨关键词策略共享查询）：
```prisma
  @@index([normalizedKey])
```

3. 执行迁移命令：
```bash
cd server
npx prisma migrate dev --name add-normalized-key
```

4. 编写一次性数据迁移脚本，为现有 Keyword 填充 type 和 normalizedKey：
```typescript
// scripts/migrate-keywords.ts
import { prisma } from '../src/db.js';
import { detectKeywordType, extractCoreEntity } from '../src/services/ai.js';

async function main() {
  const keywords = await prisma.keyword.findMany();
  for (const kw of keywords) {
    const type = detectKeywordType(kw.text);
    const normalizedKey = extractCoreEntity(kw.text, type);
    await prisma.keyword.update({
      where: { id: kw.id },
      data: { type, normalizedKey },
    });
  }
}
main();
```

**验证**：迁移后查询 DB，确认现有关键词的 type 和 normalizedKey 已填充。

---

### 5. 策略缓存键改为核心实体标识（修订版）

**优先级**：P1

**涉及文件**：
- `server/src/services/ai.ts`（新增 extractCoreEntity 函数）
- `server/src/jobs/hotspotChecker.ts`（查缓存逻辑改用 normalizedKey）

**改法**：

1. ai.ts 新增 extractCoreEntity 函数：
```typescript
export function extractCoreEntity(keyword: string, type?: KeywordType): string {
  const kwType = type || detectKeywordType(keyword);
  switch (kwType) {
    case 'stock_code':
      return `stock:${keyword}`;
    case 'stock_name': {
      // 查映射表
      const code = A_STOCK_CODE_MAP[keyword];
      return code ? `stock:${code}` : `stock_name:${keyword}`;
    }
    case 'macro_indicator': {
      // 正则提取指标核心名
      const match = keyword.match(/(CPI|PPI|GDP|PMI|GNP|GNI|M1|M2|LPR)/i);
      return match ? `macro:${match[1].toUpperCase()}` : `macro:${keyword}`;
    }
    case 'sector':
      return `sector:${keyword}`;
    case 'policy':
      return `policy:${keyword}`;
    default:
      return `generic:${keyword}`;
  }
}
```

2. hotspotChecker.ts 查缓存逻辑改为：
```typescript
// 先用 normalizedKey 查找已有策略
const normalizedKey = keyword.normalizedKey
  || extractCoreEntity(keyword.text, keyword.type as KeywordType);

if (!keyword.normalizedKey) {
  await prisma.keyword.update({
    where: { id: keyword.id },
    data: { normalizedKey },
  });
}

// 用 normalizedKey 查找同实体其他关键词的策略
const existingStrategy = await prisma.keyword.findFirst({
  where: {
    normalizedKey,
    searchStrategy: { not: null },
    id: { not: keyword.id },
  },
});
if (existingStrategy?.searchStrategy) {
  // 复用已有策略
  strategy = JSON.parse(existingStrategy.searchStrategy);
}
```

**验证**：添加关键词"美国CPI"和"CPI"，确认第二个复用第一个的策略。

---

### A3. 快车道子串匹配逻辑统一（新增）

**优先级**：P1 — 第 4 条依赖此统一

**涉及文件**：
- `server/src/config/stockCodes.ts`

**改法**：

1. 新增子串匹配函数（替代原 `ensureStockCodes` + 快车道精确匹配）：
```typescript
/**
 * 子串匹配：检查文本中是否包含已知 A 股公司名，返回匹配到的所有 [公司名, 代码] 对。
 */
export function matchStockBySubstr(text: string): Array<[string, string]> {
  const results: Array<[string, string]> = [];
  for (const [name, code] of Object.entries(A_STOCK_CODE_MAP)) {
    if (text.includes(name)) {
      results.push([name, code]);
    }
  }
  // 按名称长度降序排列，优先匹配更具体的名称（如"贵州茅台"优先于"茅台"）
  results.sort((a, b) => b[0].length - a[0].length);
  return results;
}
```

2. 保留 ensureStockCodes 但内部改用 matchStockBySubstr：
```typescript
export function ensureStockCodes(query: string, keywords: string[]): string[] {
  const result = [...keywords];
  for (const [, code] of matchStockBySubstr(query)) {
    if (!result.includes(code)) result.unshift(code);
  }
  return result;
}
```

**验证**：输入"寒武纪最近表现"，matchStockBySubstr 返回 [["寒武纪", "688256"]]。

---

## 批次 2：采集优化（4 项）

> **目标**：减少重复采集、优化 cron 频率、增强容错。
> **前置依赖**：批次 1（normalizedKey 可用于前置跳过判断）。

---

### 4. 快车道改为子串匹配 + 加搜快讯（修订版）

**优先级**：P1

**涉及文件**：
- `server/src/jobs/hotspotChecker.ts` 第 98-103 行
- `server/src/config/stockCodes.ts`（加别名）

**改法**：

1. hotspotChecker.ts 快车道判断改为子串匹配：
```typescript
// 替换原来的 if (A_STOCK_CODE_MAP[keyword.text])
const stockMatch = matchStockBySubstr(keyword.text);
if (stockMatch.length > 0) {
  const [, code] = stockMatch[0]; // 取最长的匹配
  strategy = {
    searchTerms: [code],
    targetSources: ['juchao', 'cailianshe', 'eastmoney'],
  };
  console.log(`  🚀 A-stock fast lane: "${keyword.text}" → ${code} → juchao+cailianshe+eastmoney`);
}
```

2. stockCodes.ts 添加常见别名：
```typescript
export const A_STOCK_CODE_MAP: Record<string, string> = {
  // ... 现有映射
  '茅台': '600519',       // 别名
  '平安': '601318',       // 别名
  '万科': '000002',       // 已有
  '宁德': '300750',       // 别名
  '比亚迪': '002594',     // 已有
  // 注意：别名需 >= 2 字，避免单字误匹配
};
```

3. 子串匹配命中后，额外校验关键词类型（防误匹配）：
```typescript
const kwType = detectKeywordType(keyword.text);
if (stockMatch.length > 0 && (kwType === 'stock_name' || kwType === 'stock_code' || kwType === 'generic')) {
  // 进快车道
}
```

**验证**：添加关键词"寒武纪最近表现"（type=generic），确认命中快车道搜巨潮+快讯。

---

### 6. 关键词跳过不匹配的 cron 路线（前置化）

**优先级**：P1

**涉及文件**：
- `server/src/jobs/hotspotChecker.ts` 第 90 行附近

**改法**：在关键词循环入口处，用 normalizedKey 提前查缓存策略并判断信源交集：
```typescript
for (const keyword of keywords) {
  // === 前置跳过判断 ===
  const normalizedKey = keyword.normalizedKey || extractCoreEntity(keyword.text);

  // 尝试从同实体其他关键词复用策略
  let cachedStrategy = keyword.searchStrategy;
  if (!cachedStrategy) {
    const existing = await prisma.keyword.findFirst({
      where: { normalizedKey, searchStrategy: { not: null }, id: { not: keyword.id } },
      select: { searchStrategy: true },
    });
    cachedStrategy = existing?.searchStrategy || null;
  }

  if (cachedStrategy) {
    try {
      const parsed = JSON.parse(cachedStrategy);
      const targetSources = normalizeSourceNames(parsed.targetSources || []);
      const overlap = sources.filter(s => targetSources.includes(s));
      if (overlap.length === 0) {
        console.log(`  ⏭ Early skip: "${keyword.text}" has no matching sources for this cron`);
        continue;
      }
    } catch { /* ignore */ }
  }
  // === 前置跳过判断结束 ===

  // ... 后续原有逻辑
}
```

**验证**：宏观 cron 运行时，关键词"万科"（策略只有 juchao）被前置跳过，日志输出 "Early skip"。

---

### 7. 快讯源同源共享拉取

**优先级**：P2

**涉及文件**：
- `server/src/jobs/hotspotChecker.ts` 第 151-191 行

**改法**：在关键词循环前，对快讯源预拉取并缓存：
```typescript
// 关键词循环前：预拉取快讯源数据
const fastSourceCache = new Map<SourceName, RawContent[]>();

for (const source of sources.filter(s => s === 'cailianshe' || s === 'eastmoney')) {
  try {
    const watermark = await loadWatermark(source);
    const result = await collectFromSource(source, [], watermark);
    await saveWatermark(source, result.watermark);
    fastSourceCache.set(source, result.items);
    console.log(`  📦 Pre-fetched ${source}: ${result.items.length} items`);
  } catch (error) {
    console.error(`  ${source} pre-fetch failed:`, error);
    fastSourceCache.set(source, []);
  }
}

// 关键词循环内，快讯源使用缓存而非重新拉取
for (const source of strategySources) {
  if (source === 'cailianshe' || source === 'eastmoney') {
    const cachedItems = fastSourceCache.get(source) || [];
    // ... 预匹配逻辑不变，但数据来源改为 cachedItems
  } else {
    // 非快讯源：原有逻辑不变
  }
}
```

**验证**：同时监控"CPI"和"GDP"，财联社只被拉取 1 次（日志只有 1 行 Pre-fetched）。

---

### 8. 宏观 cron 频率调整（修订版）

**优先级**：P2

**涉及文件**：
- `server/src/index.ts` 第 163-164 行

**改法**：
```typescript
// 宏观类（FRED + NBS）：每天北京时间 21:00 一次
// 美东 8:30-9:00 是宏观数据发布高峰，对应北京 20:30-21:00
cron.schedule('0 21 * * *', async () => {
  try {
    await checkMacroSources(io);
  } catch (error) {
    console.error('Macro cron failed:', error);
  }
});
```

同时更新启动日志：
```typescript
console.log(`
  🔥 金融热点监控服务启动
  📡 http://0.0.0.0:${PORT}
  🔌 WebSocket ready
  ⏱  快讯: */20min | 公告: hourly | 宏观: daily@21:00
  `);
```

**验证**：检查 cron 日志确认宏观任务只在 21:00 执行。

---

### 9. collector.py 加重试机制

**优先级**：P2

**涉及文件**：
- `server/src/services/collector.ts` 第 24-102 行

**改法**：在 collectFromSource 外层包装重试逻辑：
```typescript
const MAX_RETRIES = 3;
const RETRY_BASE_MS = 1000;

export async function collectFromSource(
  source: SourceName,
  keywords: string[],
  watermark: Watermark,
  mode: 'monitor' | 'search' = 'monitor',
  dateRange: string = '30d',
): Promise<CollectorOutput> {
  let lastError: Error | null = null;

  for (let attempt = 1; attempt <= MAX_RETRIES; attempt++) {
    try {
      return await collectFromSourceInner(source, keywords, watermark, mode, dateRange);
    } catch (error) {
      lastError = error as Error;
      if (attempt < MAX_RETRIES) {
        const delay = RETRY_BASE_MS * Math.pow(2, attempt - 1);
        console.warn(`  ⚠️ ${source} attempt ${attempt} failed, retrying in ${delay}ms...`);
        await new Promise(r => setTimeout(r, delay));
      }
    }
  }

  throw lastError;
}

// 原 collectFromSource 函数体改名为 collectFromSourceInner
async function collectFromSourceInner(...): Promise<CollectorOutput> {
  // ... 原有逻辑不变
}
```

**验证**：模拟 API 超时，确认重试 3 次后才报错。

---

## 批次 3：筛选优化（4 项）

> **目标**：提升筛选精准度，减少漏判和误判。
> **前置依赖**：批次 1 的 A0（keywordType 可用）。

---

### 10. 快讯预匹配增加默认变体

**优先级**：P1

**涉及文件**：
- `server/src/services/ai.ts` expandKeyword 函数（第 62-138 行）

**改法**：在 expandKeyword 中，AI 调用前先注入默认变体：
```typescript
export async function expandKeyword(keyword: string): Promise<string[]> {
  const cached = expansionCache.get(keyword);
  if (cached) return cached;

  const coreTerms = extractCoreTerms(keyword);
  const kwType = detectKeywordType(keyword);

  // 新增：基于关键词类型的默认变体
  const defaultVariants = getDefaultVariants(keyword, kwType);

  if (!process.env.DEEPSEEK_API_KEY || process.env.DEEPSEEK_API_KEY === 'sk-xxx') {
    const result = [...new Set([keyword, ...coreTerms, ...defaultVariants])];
    setExpansionCache(keyword, result);
    return result;
  }

  // ... AI 调用逻辑不变，但合并结果时加入 defaultVariants
  const expanded = [...new Set([keyword, ...coreTerms, ...defaultVariants, ...parsed.map(s => s.trim()).filter(Boolean)])];
}

function getDefaultVariants(keyword: string, type: KeywordType): string[] {
  const variants: string[] = [];

  switch (type) {
    case 'stock_code': {
      // 股票代码 → 附加公司名
      for (const [name, code] of Object.entries(A_STOCK_CODE_MAP)) {
        if (code === keyword) { variants.push(name); break; }
      }
      break;
    }
    case 'stock_name': {
      // 公司名 → 附加股票代码
      const code = A_STOCK_CODE_MAP[keyword];
      if (code) variants.push(code);
      break;
    }
    case 'macro_indicator': {
      // 宏观指标 → 中英文名互译
      const macroAliases: Record<string, string[]> = {
        'CPI': ['消费者物价指数', 'Consumer Price Index'],
        'PPI': ['生产者物价指数', 'Producer Price Index'],
        'GDP': ['国内生产总值', 'Gross Domestic Product'],
        'PMI': ['采购经理指数', 'Purchasing Managers Index'],
        'M2': ['广义货币供应量'],
        'LPR': ['贷款市场报价利率'],
      };
      const key = keyword.toUpperCase();
      if (macroAliases[key]) variants.push(...macroAliases[key]);
      // 反向：中文→英文
      for (const [eng, chs] of Object.entries(macroAliases)) {
        if (chs.some(c => keyword.includes(c))) variants.push(eng);
      }
      break;
    }
    case 'sector': {
      // 板块 → 常见别称
      const sectorAliases: Record<string, string[]> = {
        'AI': ['人工智能', 'Artificial Intelligence'],
        '人工智能': ['AI'],
        '新能源车': ['新能源汽车', '电动车', 'EV'],
        '半导体': ['芯片', 'chip', 'semiconductor'],
      };
      if (sectorAliases[keyword]) variants.push(...sectorAliases[keyword]);
      break;
    }
  }

  return variants;
}
```

**验证**：关键词"AI"的扩展结果包含"人工智能"；关键词"CPI"包含"消费者物价指数"。

---

### 11. 巨潮黑名单"关于公司"细化

**优先级**：P2

**涉及文件**：
- `scripts/sources/juchao.py` 第 40 行

**改法**：
```python
ROUTINE_BLACKLIST = [
    # ... 其他不变
    # "关于公司",  # 删除原来的宽泛匹配
    "关于公司股份",       # 股份变动类例行公告
    "关于公司名称",       # 更名类例行公告
    "关于公司章程",       # 章程修订类例行公告
    "关于公司注册",       # 注册地址变更
    "关于公司经营范围",   # 经营范围变更
]
```

**验证**：标题"关于公司重大合同"不被过滤；"关于公司股份"被过滤。

---

### 12. detectKeywordType() 接入 filter.ts，按类型分阈值

**优先级**：P1

**涉及文件**：
- `server/src/utils/filter.ts`（全文件重写）
- `server/src/jobs/hotspotChecker.ts` 第 236 行（传参）

**改法**：

1. filter.ts 新增 keywordType 参数和分阈值逻辑：
```typescript
import type { AIAnalysis, FilterResult, RawContent } from '../types.js';

type KeywordType = 'stock_code' | 'stock_name' | 'sector' | 'macro_indicator' | 'policy' | 'generic';

interface FilterThresholds {
  notSubstantialAndLowRel: number;  // 非实质+相关性低于此值 → 丢弃
  lowRel: number;                   // 相关性低于此值 → 丢弃
  notMentionedAndRel: number;       // 未提及+相关性低于此值 → 丢弃
}

const THRESHOLDS: Record<KeywordType, FilterThresholds> = {
  stock_code:      { notSubstantialAndLowRel: 20, lowRel: 25, notMentionedAndRel: 40 },
  stock_name:      { notSubstantialAndLowRel: 20, lowRel: 25, notMentionedAndRel: 40 },
  sector:          { notSubstantialAndLowRel: 15, lowRel: 20, notMentionedAndRel: 25 },
  macro_indicator: { notSubstantialAndLowRel: 15, lowRel: 20, notMentionedAndRel: 25 },
  policy:          { notSubstantialAndLowRel: 15, lowRel: 20, notMentionedAndRel: 25 },
  generic:         { notSubstantialAndLowRel: 20, lowRel: 25, notMentionedAndRel: 40 },
};

export function shouldFilter(
  item: RawContent,
  analysis: AIAnalysis,
  keywordType: KeywordType = 'generic'
): FilterResult {
  const t = THRESHOLDS[keywordType] || THRESHOLDS.generic;

  if (!analysis.isSubstantial && analysis.relevance < t.notSubstantialAndLowRel) {
    return { pass: false, reason: 'not-substantial-low-relevance' };
  }
  if (analysis.relevance < t.lowRel) {
    return { pass: false, reason: 'low-relevance' };
  }
  if (!analysis.keywordMentioned && analysis.relevance < t.notMentionedAndRel) {
    return { pass: false, reason: 'not-mentioned-low-relevance' };
  }
  return { pass: true, reason: 'ok' };
}
```

2. hotspotChecker.ts 传参处修改：
```typescript
// 第 236 行
const filterResult = shouldFilter(item, analysis, keyword.type as KeywordType);
```

**验证**：板块关键词"半导体"的热点，未提及关键词但 relevance=28 时通过筛选（阈值 25）；generic 关键词同样情况被过滤（阈值 40）。

---

### 13. AI fallback 默认分数提高

**优先级**：P1

**涉及文件**：
- `server/src/services/ai.ts` 第 388-401 行（无 API Key fallback）+ 第 447-461 行（AI 失败 fallback）

**改法**：

1. 无 API Key fallback：
```typescript
return {
  eventType: 'other',
  isSubstantial: matchResult.matched,
  relevance: matchResult.matched ? 60 : 30,   // 原 40/10
  relevanceReason: '未配置 DeepSeek API Key',
  keywordMentioned: matchResult.matched,
  importance: matchResult.matched ? 'medium' : 'low',  // 原 'low'/'low'
  importanceReason: '未配置 AI 服务',
  summary: content.slice(0, 80),
  affectedHoldings: false,
  eventFingerprint: '',
};
```

2. AI 调用失败 fallback：
```typescript
return {
  eventType: 'other',
  isSubstantial: matchResult.matched,
  relevance: matchResult.matched ? 50 : 25,   // 原 30/10
  relevanceReason: 'AI 分析失败，使用默认分数',
  keywordMentioned: matchResult.matched,
  importance: matchResult.matched ? 'medium' : 'low',  // 原 'low'/'low'
  importanceReason: 'AI 分析失败',
  summary: content.slice(0, 80),
  affectedHoldings: false,
  eventFingerprint: '',
};
```

**验证**：无 API Key 时，提及关键词的热点 relevance=60，不会被阈值过滤丢掉。

---

## 批次 4：产出优化 + 全局改造（4 项）

> **目标**：优化数据保留策略、配额分配、去重窗口和并发性能。
> **前置依赖**：批次 3（筛选逻辑稳定后再调产出层）。

---

### 14. 配额按关键词类型分档

**优先级**：P2

**涉及文件**：
- `server/src/jobs/hotspotChecker.ts` 第 11-12 行 + 第 195-209 行

**改法**：

1. 替换固定常量为函数：
```typescript
function getQuotaByType(keywordType: string): { maxPerSource: number; maxTotal: number } {
  switch (keywordType) {
    case 'stock_code':
    case 'stock_name':
      return { maxPerSource: 15, maxTotal: 80 };
    case 'sector':
      return { maxPerSource: 10, maxTotal: 50 };
    case 'macro_indicator':
      return { maxPerSource: 3, maxTotal: 10 };
    case 'policy':
      return { maxPerSource: 10, maxTotal: 50 };
    default:
      return { maxPerSource: 10, maxTotal: 50 };
  }
}
```

2. 配额控制处使用：
```typescript
const { maxPerSource, maxTotal } = getQuotaByType(keyword.type);

for (const item of sortedItems) {
  const count = quotaBySource.get(item.source) || 0;
  if (count >= maxPerSource) continue;
  if (quotaItems.length >= maxTotal) break;
  quotaBySource.set(item.source, count + 1);
  quotaItems.push(item);
}
```

**验证**：宏观关键词最多 10 条进 AI；股票关键词最多 80 条。

---

### 15. 跨源去重窗口延长（修订版）

**优先级**：P2

**涉及文件**：
- `server/src/jobs/hotspotChecker.ts` 第 247-254 行

**改法**：30 分钟 → 3 小时：
```typescript
const dup = await prisma.hotspot.findFirst({
  where: {
    eventFingerprint: analysis.eventFingerprint,
    createdAt: {
      gte: new Date(Date.now() - 3 * 60 * 60 * 1000),  // 原 30 * 60 * 1000
    },
  },
});
```

**验证**：同一事件 2 小时后被另一信源报道，确认被去重合并而非重复入库。

---

### 16. 数据保留期按重要性分级

**优先级**：P2

**涉及文件**：
- `server/src/index.ts` 第 172-189 行

**改法**：
```typescript
// 数据清理：每天凌晨 3 点，按重要性分级保留
cron.schedule('0 3 * * *', async () => {
  console.log('🧹 Running daily cleanup...');
  try {
    const lowCutoff = new Date(Date.now() - 3 * 24 * 60 * 60 * 1000);
    const medHighCutoff = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000);

    // 先删通知（级联依赖）
    // low 重要性：3 天
    const lowHotspotIds = await prisma.hotspot.findMany({
      where: { importance: 'low', createdAt: { lt: lowCutoff } },
      select: { id: true },
    });
    if (lowHotspotIds.length > 0) {
      const ids = lowHotspotIds.map(h => h.id);
      await prisma.notification.deleteMany({ where: { hotspotId: { in: ids } } });
      await prisma.hotspot.deleteMany({ where: { id: { in: ids } } });
    }

    // medium + high：7 天
    const medHighHotspotIds = await prisma.hotspot.findMany({
      where: {
        importance: { in: ['medium', 'high'] },
        createdAt: { lt: medHighCutoff },
      },
      select: { id: true },
    });
    if (medHighHotspotIds.length > 0) {
      const ids = medHighHotspotIds.map(h => h.id);
      await prisma.notification.deleteMany({ where: { hotspotId: { in: ids } } });
      await prisma.hotspot.deleteMany({ where: { id: { in: ids } } });
    }

    console.log(`✅ Cleanup done: ${lowHotspotIds.length} low(3d) + ${medHighHotspotIds.length} med/high(7d) removed`);
  } catch (error) {
    console.error('❌ Daily cleanup failed:', error);
  }
});
```

**验证**：3 天前的 low 热点被清理，7 天前的 medium/high 热点被清理，3 天前的 medium 热点保留。

---

### 17. 关键词串行改并发

**优先级**：P3（最后做，风险最高）

**涉及文件**：
- `server/src/jobs/hotspotChecker.ts` checkSources 函数

**改法**：

1. 引入并发池工具：
```typescript
async function runWithConcurrency<T>(
  items: T[],
  fn: (item: T) => Promise<void>,
  concurrency: number = 3
): Promise<void> {
  const executing = new Set<Promise<void>>();
  for (const item of items) {
    const p = fn(item).then(() => { executing.delete(p); });
    executing.add(p);
    if (executing.size >= concurrency) {
      await Promise.race(executing);
    }
  }
  await Promise.all(executing);
}
```

2. 改造关键词循环：
```typescript
// for (const keyword of keywords) { ... }
// 改为：
await runWithConcurrency(keywords, async (keyword) => {
  // ... 原有单个关键词处理逻辑
}, 3);  // 并发数 3，避免 API 限频
```

3. **关键：DB 写入冲突防护**（A2 遗漏项）

跨源去重的 eventFingerprint 查询+写入存在竞态条件。解决方案：在 eventFingerprint 上加唯一约束 + 事务：

schema.prisma：
```prisma
model Hotspot {
  // ... 现有字段
  @@unique([eventFingerprint])  // 新增：fingerprint 唯一约束（允许 null）
}
```

hotspotChecker.ts 去重逻辑改为 try-catch 唯一约束冲突：
```typescript
if (analysis.eventFingerprint) {
  try {
    const existing = await prisma.hotspot.findFirst({
      where: {
        eventFingerprint: analysis.eventFingerprint,
        createdAt: { gte: new Date(Date.now() - 3 * 60 * 60 * 1000) },
      },
    });

    if (existing) {
      // 合并逻辑不变
      isPrimary = false;
      // ...
      continue;
    }
  } catch (error) {
    // 并发竞态：另一个关键词已创建了同 fingerprint 的记录
    console.log(`  ⏭ Duplicate fingerprint race: ${analysis.eventFingerprint}`);
    continue;
  }
}

// 入库时也加 try-catch
try {
  const hotspot = await prisma.hotspot.create({ data: { ... } });
  // ... 后续推送逻辑
} catch (error: any) {
  if (error.code === 'P2002') {
    // 唯一约束冲突（url+source 或 eventFingerprint），跳过
    console.log(`  ⏭ Duplicate on create, skipping`);
    continue;
  }
  throw error;
}
```

**注意**：eventFingerprint 可以为 null（AI 未生成时），PostgreSQL 的唯一约束对 null 值不生效，不会影响无指纹的记录。

**验证**：10 个关键词并发处理，无重复入库，无 DB 写入冲突报错。

---

## 三、全局注意事项

### Prisma 迁移顺序

1. 批次 1 的 A1 先执行迁移（加 normalizedKey 字段 + 索引）
2. 批次 4 的第 17 条再执行第二次迁移（eventFingerprint 唯一约束）
3. 两次迁移分开执行，避免一次性大变更

### 回归测试清单

每个批次完成后，执行以下验证：

1. **服务启动**：`npm run dev` 无报错
2. **手动触发全源检查**：`POST /api/check-hotspots`，确认有新热点入库
3. **WebSocket 推送**：前端实时收到新热点
4. **筛选功能**：信源/重要性/事件性质筛选均有效
5. **通知面板**：不同重要性热点显示不同颜色
6. **数据清理**：确认 cron 正常执行

### 风险控制

| 批次 | 风险点 | 缓解措施 |
|------|--------|---------|
| 批次 0 | Bug2 改前端选项值后，用户书签的旧 URL 参数失效 | 后端 toLowerCase() 归一化已覆盖 |
| 批次 1 | Prisma 迁移可能因生产数据量失败 | 先在本地 dev.db 测试，生产用 `prisma migrate deploy` |
| 批次 2 | 快讯源共享拉取后水位线只写一次 | 水位线在预拉取时写入，后续关键词复用缓存不重复写 |
| 批次 3 | 分阈值可能误放过低质量热点 | 先在测试环境观察 1-2 天，根据实际数据微调阈值 |
| 批次 4 | 并发改造引入竞态条件 | eventFingerprint 唯一约束 + try-catch P2002 |

---

## 四、修改总览表

| 批次 | 编号 | 条目 | 涉及文件 | 优先级 |
|------|------|------|---------|--------|
| 0 | Bug1 | WebSocket 事件名 | hotspotChecker.ts | P0 |
| 0 | Bug2 | 筛选值大小写 | FilterBar.tsx, hotspots.ts | P0 |
| 0 | Bug3 | FRED 崩溃 | fred.py | P0 |
| 0 | Bug4 | isSubstantial 忽略 | hotspots.ts | P0 |
| 0 | Bug5 | 通知颜色分类 | hotspotChecker.ts, HotspotsPage.tsx | P1 |
| 1 | A0 | keywordType 自动填充 | keywords.ts, ai.ts | P0 |
| 1 | A1 | Prisma 迁移 | schema.prisma + 迁移脚本 | P0 |
| 1 | 5 | normalizedKey 策略共享 | ai.ts, hotspotChecker.ts | P1 |
| 1 | A3 | 子串匹配统一 | stockCodes.ts | P1 |
| 2 | 4 | 快车道子串+快讯 | hotspotChecker.ts, stockCodes.ts | P1 |
| 2 | 6 | 前置跳过 | hotspotChecker.ts | P1 |
| 2 | 7 | 快讯共享拉取 | hotspotChecker.ts | P2 |
| 2 | 8 | 宏观 cron 调整 | index.ts | P2 |
| 2 | 9 | 采集重试 | collector.ts | P2 |
| 3 | 10 | 预匹配默认变体 | ai.ts | P1 |
| 3 | 11 | 巨潮黑名单细化 | juchao.py | P2 |
| 3 | 12 | 分阈值过滤 | filter.ts, hotspotChecker.ts | P1 |
| 3 | 13 | AI fallback 提分 | ai.ts | P1 |
| 4 | 14 | 配额分档 | hotspotChecker.ts | P2 |
| 4 | 15 | 去重窗口 3h | hotspotChecker.ts | P2 |
| 4 | 16 | 保留期分级 | index.ts | P2 |
| 4 | 17 | 并发改造 | hotspotChecker.ts, schema.prisma | P3 |
