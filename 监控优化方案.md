监控优化方案

  现状诊断

  当前监控链路，每条关键词每轮次都在跟三个问题打仗：

  关键词 → AI策略(可能幻觉) → 采集(可能搜错) → 配额(5条) → 阈值(太严) → 入库
    ↑                                              ↑           ↑
   问题1: 万科→600519永久缓存                    问题2       问题3

  方案：四个改动，按优先级排

  ---
  改动一：阈值放宽（filter.ts）

  当前 vs 目标：

  ┌───────────────┬───────────┬───────────┬────────────────────────────────────────────────┐
  │     规则      │   当前    │   目标    │                      理由                      │
  ├───────────────┼───────────┼───────────┼────────────────────────────────────────────────┤
  │ 简单相关性    │ < 40 丢弃 │ < 25 丢弃 │ 监控用户愿意接受更多信号                       │
  ├───────────────┼───────────┼───────────┼────────────────────────────────────────────────┤
  │ 未提及+相关性 │ < 60 丢弃 │ < 40 丢弃 │ 公告里没直接提关键词但有传导关联的，现在全丢了 │
  ├───────────────┼───────────┼───────────┼────────────────────────────────────────────────┤
  │ 非实质内容    │ 丢弃      │ 保留      │ 例行公告对搜索无价值，但监控者可能想看         │
  ├───────────────┼───────────┼───────────┼────────────────────────────────────────────────┤
  │ 低重要+快讯   │ 丢弃      │ 保留      │ 同上                                           │
  └───────────────┴───────────┴───────────┴────────────────────────────────────────────────┘

  改动最小：改两行数字 + 删两条规则。会影响所有关键词——信噪比会下降，但监控者宁可多看不要漏看。

  ---
  改动二：A 股关键词走快车道（hotspotChecker.ts）

  当前流程：
  关键词"万科" → AI生成策略(可能错误) → 缓存到DB → 用策略搜 → 硬编码补代码 → 采集中混杂错误代码

  问题：AI 已经把"万科→600519"缓存了，硬编码虽补了 000002，但 600519 也在 searchTerms 里，搜出茅台公告当噪音。

  目标流程：
  关键词"万科" → 检测到在 A_STOCK_CODE_MAP 中 → 跳过 AI → searchTerms=["000002"], targetSources=["juchao"]

  实现：在策略生成逻辑前加一个判断——如果 keyword.text 在硬编码映射表的 key 中，直接用映射值，不调 AI。非 A 股关键词（GPU、新能源等）照常走 AI 策略。

  这个改动只影响 A 股公司名关键词，其他类型关键词不受影响。

  ---
  改动三：配额放宽（hotspotChecker.ts）

  MAX_PER_SOURCE: 5 → 10
  MAX_TOTAL:      30 → 50

  juchao 一轮搜 50 条公告，当前只看 5 条，剩下 45 条直接丢弃。宁德时代 50 条全被阈值过滤的前提是——它只看 5 条，而 5 条恰好全是例行月报。如果看 10 条，后 5
  条里可能就有质押公告、回购公告等有效内容。

  配合阈值放宽一起生效。

  ---
  改动四：旧策略清理 + 错误纠正（DB 操作 + hotspotChecker.ts）

  两步：
  1. UPDATE Keyword SET searchStrategy = NULL — 清掉含 dongcaiGlobal、eastmoney_global 等错误信源名的旧策略
  2. 在从 DB 读策略时加一层 normalize——旧策略里的错误名自动纠正

  之后 A 股关键词走快车道，不会再调用 AI 生成新策略，彻底避免缓存污染。

  ---
  影响范围

  ┌────────────┬────────────────────────┬─────────────────────────────────────────┐
  │    改动    │        涉及文件        │                  影响                   │
  ├────────────┼────────────────────────┼─────────────────────────────────────────┤
  │ 阈值放宽   │ filter.ts              │ 全局，所有关键词信号增多                │
  ├────────────┼────────────────────────┼─────────────────────────────────────────┤
  │ A 股快车道 │ hotspotChecker.ts      │ 仅 A 股公司名关键词，信号精准度大幅提升 │
  ├────────────┼────────────────────────┼─────────────────────────────────────────┤
  │ 配额放宽   │ hotspotChecker.ts      │ 全局，每轮更多数据进 AI 分析            │
  ├────────────┼────────────────────────┼─────────────────────────────────────────┤
  │ 策略清理   │ DB + hotspotChecker.ts │ 一次性，消除历史错误缓存                │
  └────────────┴────────────────────────┴─────────────────────────────────────────┘

  没有新增依赖，不改数据结构，不影响前端。

  ---
  预期效果

  以当前 9 个活跃关键词为例：

  ┌──────────┬─────────────────────┬────────┬────────────────────────────────────────────┐
  │  关键词  │        当前         │  改后  │                    原因                    │
  ├──────────┼─────────────────────┼────────┼────────────────────────────────────────────┤
  │ 贵州茅台 │ 3 条热点            │ 5-8 条 │ 配额+阈值放宽，更多公告入库                │
  ├──────────┼─────────────────────┼────────┼────────────────────────────────────────────┤
  │ 宁德时代 │ 0 条（50 条全过滤） │ 3-5 条 │ 阈值放宽，质押/债券公告能过                │
  ├──────────┼─────────────────────┼────────┼────────────────────────────────────────────┤
  │ 寒武纪   │ 0 条                │ 2-3 条 │ 快车道确保搜 688256，不走公司名全文搜索    │
  ├──────────┼─────────────────────┼────────┼────────────────────────────────────────────┤
  │ 万科     │ 0 条（代码错误）    │ 3-5 条 │ 快车道确保 000002，不再被 AI 映射为 600519 │
  ├──────────┼─────────────────────┼────────┼────────────────────────────────────────────┤
  │ GPU      │ 1 条                │ 2-3 条 │ 阈值放宽，更多 GPU 相关公告入库            │
  └──────────┴─────────────────────┴────────┴────────────────────────────────────────────┘

  预估整体命中率从当前 ~44%（4/9 个关键词有产出）提升到 ~70-80%。

  ---
  这个方案可以吗？确认后我一次性改完。
