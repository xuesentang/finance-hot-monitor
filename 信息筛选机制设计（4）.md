# 金融热点监控工具 — 信息筛选机制设计 v4.0（最终版）

> 本文档定义从 6 个权威信源采集信息到最终推送给用户的完整筛选链路。
> 基于 v2.0 方案，修正 v3.0 整合版引入的新问题，补充缺失的关键设计。

---

## 一、设计前提与核心差异

### 1.1 与 yupi-hot-monitor 的本质差异

| 维度 | yupi-hot-monitor（通用热点） | 本项目（金融热点） |
|------|---------------------------|------------------|
| **信源性质** | UGC 平台（Twitter/微博/B站），内容质量参差不齐 | 权威机构（SEC/巨潮/财联社），发布即事实 |
| **质量验证方式** | 依赖互动数据（点赞/转发/粉丝数）验证内容质量 | 机构背书即质量保证，无需互动指标验证 |
| **信息类型** | 同质化（帖子/推文/视频），差异主要在传播力 | 异质化（公告/快讯/宏观数据），差异在事件类型和影响层级 |
| **时效性要求** | 小时级，7 天窗口可接受 | 快讯分钟级，公告天级，宏观按发布周期 |
| **关键词类型** | 通用词（产品名/人名/话题） | 金融实体（股票代码/公司名/宏观指标/板块/政策） |
| **核心痛点** | 垃圾堆里找金子 | 金子太多，要快速识别哪块金子影响持仓 |

### 1.2 设计原则

1. **机构信源 = 质量免检**：不设置互动数据阈值，不判断"真假"，但需判断"是否例行/实质"
2. **事件类型决定重要性**：同一关键词下，"高管变更"和"例行董事会决议"的重要性天差地别
3. **金融传导链感知**：宁德时代 → 新能源板块 → 创业板 → 大盘，AI 需理解这种传导关系
4. **信源按类型分层处理**：公告走事件检测，快讯走实时推送，宏观走数据变更检测
5. **跨源去重是刚需**：财联社快讯常常是巨潮公告的二次报道，同一事件可能多个信源命中
6. **避免信息过载**：同一公司多条公告合并、同一事件多源报道合并、低重要性快讯过滤

---

## 二、完整筛选链路（六层漏斗）

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        六层信息筛选漏斗                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  第0层：信源采集层（按信源特性定制采集策略）                                  │
│     ├─ SEC EDGAR：submissions 轮询 + filing 类型过滤                        │
│     ├─ FRED：发布日历驱动 + 序列 ID 变化检测                                 │
│     ├─ NBS：发布日历驱动 + 指标代码轮询                                      │
│     ├─ 巨潮公告：公告列表轮询 + 公告类型过滤                                 │
│     ├─ 财联社快讯：高频轮询 + 时间戳去重                                     │
│     └─ 东财资讯：高频轮询 + 时间戳去重                                       │
│                    ↓                                                        │
│  第1层：信源内过滤（源头去噪，减少下游处理量）                                │
│     ├─ 公告类：过滤纯提示性公告（预约披露时间等）+ 过滤例行决议               │
│     ├─ 快讯类：过滤已推送过的（时间戳/ID 去重）                              │
│     ├─ 宏观类：过滤未变更的（数据值未变化不触发）                             │
│     └─ 全源：频率限制（遵守各源 API 限制）                                   │
│                    ↓                                                        │
│  第2层：关键词匹配层（快速筛选与关键词相关的内容）                             │
│     ├─ Query Expansion（金融实体专用扩展策略）                               │
│     ├─ 预匹配（文本匹配 + 股票代码匹配 + 板块成分股匹配）                      │
│     ├─ 关键词子集关系检测（避免重复分析）                                     │
│     └─ 不匹配的内容直接丢弃，不进入 AI 分析                                   │
│                    ↓                                                        │
│  第3层：AI 智能分析层（核心判断引擎）                                        │
│     ├─ 事件类型识别（Event Classification）                                  │
│     ├─ 相关性评分（0-100，含传导链感知）                                     │
│     ├─ 重要性分级（low/medium/high，基于事件类型 × 影响程度）                 │
│     ├─ 实质判断（isSubstantial：过滤例行/历史回顾类内容）                     │
│     ├─ 核心事实摘要（一句话中文，说明发生了什么）                              │
│     └─ 事件指纹生成（用于跨源去重）                                          │
│                    ↓                                                        │
│  第4层：阈值过滤层（业务规则兜底）                                           │
│     ├─ !isSubstantial → 丢弃                                               │
│     ├─ relevance < 40 → 丢弃（金融领域信源质量高，阈值可降低）                │
│     ├─ !keywordMentioned && relevance < 60 → 丢弃                           │
│     └─ importance === 'low' && 信源为快讯类 → 丢弃（防刷屏）                  │
│                    ↓                                                        │
│  第5层：跨源去重与聚合层（同一事件多源报道去重）                              │
│     ├─ 事件指纹匹配（AI 生成的指纹）                                         │
│     ├─ 时间窗口聚合（同一事件 30 分钟内多源报道合并）                          │
│     ├─ 同一公司多公告批处理（30 分钟内合并）                                  │
│     └─ 主源选择（公告源 > 快讯源，保留权威源版本）                            │
│                    ↓                                                        │
│  第6层：推送决策层（决定何时、如何推送给用户）                                │
│     ├─ high 重要性 → 立即 WebSocket 推送                                     │
│     ├─ medium 重要性 → 立即推送（但前端标记为"中等"）                         │
│     ├─ low 重要性 → 不推送，仅入库（用户手动查看时可看到）                     │
│     └─ 同一事件 30 分钟内只推送一次（防重复打扰）                             │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、第0层：信源采集层

### 3.1 采集策略按信源类型分类

金融信源不是"搜索关键词返回结果"模式，而是"轮询数据源发现新内容"模式。每个信源的采集策略必须与其内容特性匹配。

#### 3.1.1 公告类信源（SEC EDGAR、巨潮资讯）

**采集模式**：轮询 submissions/公告列表 → 检测新 filing/公告 → 拉取详情

```typescript
// SEC EDGAR 采集逻辑
async function pollEdgar(keywords: string[]): Promise<RawContent[]> {
  // 1. 对每个监控的公司（通过关键词映射到 CIK），轮询 submissions
  // 2. 检测最近 filingDate 的新文件
  // 3. 过滤 filing 类型：8-K（突发事件）> 13F（机构持仓）> 10-Q（季报）> 10-K（年报）
  // 4. 拉取 filing 详情文本
  // 5. 返回结构化数据
}
```

** filing 类型优先级 **：

| 类型 | 优先级 | 说明 |
|------|--------|------|
| 8-K | P0 | 重大事件即时披露（并购/高管变更/破产等） |
| 13F | P1 | 机构季度持仓披露（巴菲特/索罗斯等） |
| 6-K | P1 | 外国公司重大事件 |
| 10-Q | P1 | 季度财报（含实质信息，需 AI 分析差异） |
| 10-K | P1 | 年度财报（含实质信息，需 AI 分析差异） |
| DEF 14A | P2 | 代理声明（含高管薪酬/董事会提名） |
| S-1/424B | P2 | IPO/增发 |
| 8-K/A | P1 | 8-K 修正案（可能包含新的实质信息，需 AI 判断） |
| 其他 | P3 | 例行披露 |

> **修正说明（v3→v4）**：10-K/10-Q 从 P2 提升为 P1，8-K/A 从黑名单移除改为 P1。定期财报不是例行公告，但 AI 需要重点关注与上期披露的差异部分。

**巨潮公告类型优先级**：

| 类型 | 优先级 | 说明 |
|------|--------|------|
| 重大资产重组 | P0 | 并购/重组/借壳 |
| 股权变动/增持减持 | P0 | 大股东变化 |
| 高管变动 | P0 | 董事长/总经理变更 |
| 业绩预告/快报 | P1 | 业绩大幅变动 |
| 定增/配股 | P1 | 再融资 |
| 定期报告（年报/半年报/季报） | P1 | 需 AI 分析差异部分 |
| 股权激励 | P2 | 员工持股计划 |
| 董事会决议 | P3 | 例行会议（过滤） |
| 股东大会通知 | P3 | 例行会议（过滤） |
| 预约披露时间 | P3 | 纯提示性信息（过滤） |

#### 3.1.2 快讯类信源（财联社、东财全球资讯）

**采集模式**：高频轮询 → 时间戳/ID 去重 → 新内容进入处理队列

```typescript
// 财联社快讯采集逻辑
async function pollCailianshe(): Promise<RawContent[]> {
  // 1. 每 2 分钟轮询一次快讯列表接口
  // 2. 记录上次拉取的最大时间戳/ID
  // 3. 只处理时间戳 > 上次记录的内容
  // 4. 返回新内容列表
}
```

**关键设计**：快讯类信源必须维护"水位线"（lastTimestamp / lastId），只处理增量内容，不做全量搜索。

#### 3.1.3 宏观数据信源（FRED、NBS）

**采集模式**：发布日历驱动 → 指标序列轮询 → 数据值变化检测

```typescript
// FRED 采集逻辑
async function pollFred(keywords: string[]): Promise<RawContent[]> {
  // 1. 关键词映射到 FRED Series ID
  //    "CPI" → "CPIAUCSL"（美国CPI）
  //    "非农就业" → "PAYEMS"（非农就业人数）
  //    "利率" → "FEDFUNDS"（联邦基金利率）
  //
  // 2. 对每个 Series ID，调用 observations API
  // 3. 检测最新 observation 的 date 是否 > 上次记录的 date
  // 4. 如果是，拉取最新值，计算环比/同比变化
  // 5. 返回结构化数据（含前值、预期值、实际值、变化幅度）
}
```

**宏观数据特殊处理**：
- 不走"新鲜度过滤"，走"数据更新检测"
- 同一指标在两次发布之间，数据值不变，不触发分析
- 需要维护"指标 → 最新 observation date"的映射表
- **新增**：采集时需要获取"市场预期值"（来自 Bloomberg/Reuters 共识预期，或上次发布后的市场预测），与"实际值"一并传给 AI，用于判断"超预期/低于预期/符合预期"

### 3.2 频率限制配置

| 信源 | 轮询频率 | 频率限制 | 说明 |
|------|----------|----------|------|
| SEC EDGAR | 每 10 分钟 | 10 req/s | 官方限制，需 User-Agent |
| FRED | 每 1 小时 | 60 次/小时 | 官方限制，需 API Key |
| NBS | 每 1 小时 | 保守 2-3 秒间隔 | 公开 API，无明确限制 |
| 巨潮资讯 | 每 10 分钟 | 保守 2-3 秒间隔 | 直连 HTTP |
| 财联社 | 每 2 分钟 | 保守 2-3 秒间隔 | 直连 HTTP |
| 东财全球资讯 | 每 2 分钟 | 保守 2-3 秒间隔 | 直连 HTTP |

---

## 四、第1层：信源内过滤

### 4.1 公告类：纯提示性公告 + 例行公告过滤

**问题**：上市公司每天发布大量无实质信息的公告（预约披露时间提醒、董事会决议、股东大会通知），这些内容对投资决策几乎没有即时影响，如果全部进入 AI 分析队列，会造成大量无效调用。

**策略**：在采集阶段就根据公告类型进行过滤。

```typescript
// 纯提示性公告类型（直接过滤，不进 AI）
const PURE_NOTICE_TYPES = [
  // 巨潮
  '预约披露时间',      // 预约披露时间提醒
  '停牌公告',          // 例行停牌（除非涉及重组）
  '复牌公告',          // 例行复牌
  
  // EDGAR
  'PRE 14A',           // 代理声明预告
  'DEFA14A',           // 代理声明补充
  '424B',              // 招股说明书附属文件
];

// 例行公告类型（直接过滤，不进 AI）
const ROUTINE_FILING_TYPES = [
  // 巨潮
  '董事会决议',        // 例行会议
  '股东大会通知',      // 例行会议
  '监事会决议',        // 例行会议
  '独立董事意见',      // 例行文件
  
  // EDGAR
  'SUPPL',             // 补充材料（无新信息）
];

function shouldSkipFiling(filingType: string, title: string): boolean {
  // 检查是否在纯提示性列表中
  if (PURE_NOTICE_TYPES.some(t => filingType.includes(t) || title.includes(t))) {
    return true;
  }
  // 检查是否在例行公告列表中
  if (ROUTINE_FILING_TYPES.some(t => filingType.includes(t))) {
    return true;
  }
  return false;
}
```

**重要说明**：
- 10-K/10-Q **不在这个列表里**，它们正常进入 AI 分析，但 Prompt 会提示 AI"重点关注与上一期披露的差异部分"
- 8-K/A **不在这个列表里**，它正常进入 AI 分析，AI 需要判断"此修正案与原始 8-K 的差异是否实质性"

### 4.2 快讯类：时间戳去重

```typescript
// 维护每个快讯源的水位线
const sourceWatermarks: Record<string, number> = {
  cailianshe: 0,   // 上次拉取的最大时间戳
  eastmoney: 0
};

function filterNewItems(items: RawContent[], source: string): RawContent[] {
  const watermark = sourceWatermarks[source] || 0;
  const newItems = items.filter(item => item.timestamp > watermark);
  if (newItems.length > 0) {
    sourceWatermarks[source] = Math.max(...newItems.map(i => i.timestamp));
  }
  return newItems;
}
```

### 4.3 宏观类：数据值变化检测

```typescript
// 维护每个宏观指标的最新 observation date
const macroLastObservations: Record<string, string> = {};

function hasNewData(seriesId: string, latestDate: string): boolean {
  const lastDate = macroLastObservations[seriesId];
  if (!lastDate || latestDate > lastDate) {
    macroLastObservations[seriesId] = latestDate;
    return true;
  }
  return false;
}
```

---

## 五、第2层：关键词匹配层

### 5.1 金融实体 Query Expansion 策略

通用项目的 Query Expansion 只考虑"文本变体"（大小写、空格、别称）。金融场景需要扩展为"金融实体关联"。

#### 5.1.1 关键词类型识别

用户输入的关键词需要先做类型识别，不同类型走不同的扩展策略：

```typescript
type KeywordType = 'stock_code' | 'stock_name' | 'sector' | 'macro_indicator' | 'policy' | 'generic';

function detectKeywordType(keyword: string): KeywordType {
  // A股代码：6位数字，以 0/3/6 开头
  if (/^[0-3|6]\d{5}$/.test(keyword)) return 'stock_code';
  // 美股代码：1-5 位字母
  if (/^[A-Z]{1,5}$/.test(keyword)) return 'stock_code';
  // 板块名：常见板块关键词
  if (['新能源', '半导体', '医药', '白酒', '银行', '地产'].includes(keyword)) return 'sector';
  // 宏观指标：常见宏观关键词
  if (['CPI', 'PPI', 'GDP', 'PMI', '非农就业', '美联储', '加息', '降息'].includes(keyword)) return 'macro_indicator';
  // 政策：含"政策"/"监管"/"证监会"等
  if (keyword.includes('政策') || keyword.includes('监管')) return 'policy';
  // 默认
  return 'generic';
}
```

#### 5.1.2 按类型的扩展策略

| 关键词类型 | 扩展内容 | 示例 |
|-----------|----------|------|
| `stock_code` (A股) | 股票代码 + 公司全称 + 公司简称 + 拼音缩写 | `300750` → `["300750", "宁德时代", "CATL", "宁德"]` |
| `stock_code` (美股) | 股票代码 + 公司全称 + 常见简称 | `AAPL` → `["AAPL", "Apple", "苹果", "苹果公司"]` |
| `stock_name` | 公司名 + 股票代码 + 常见简称 + 关联板块 | `宁德时代` → `["宁德时代", "300750", "CATL", "新能源", "锂电池"]` |
| `sector` | 板块名 + 成分股代码列表 + 上下游板块 | `新能源` → `["新能源", "300750", "002594", "比亚迪", "锂电池", "光伏"]` |
| `macro_indicator` | 指标名 + 英文缩写 + 发布机构 + 关联市场 | `CPI` → `["CPI", "消费者价格指数", "美联储", "通胀", "FOMC"]` |
| `policy` | 政策关键词 + 关联机构 + 影响板块 | `新能源政策` → `["新能源政策", "工信部", "新能源", "补贴", "双碳"]` |
| `generic` | 通用文本扩展（同原项目） | `美联储` → `["美联储", "FED", "Federal Reserve", "鲍威尔"]` |

**关键差异**：金融实体的扩展不是"找文本变体"，而是"找金融关联"。比如监控"新能源"板块，不仅要匹配"新能源"这个词，还要匹配板块内的所有成分股代码和公司名。

#### 5.1.3 成分股映射表（动态获取）

> **修正说明（v2→v4）**：不再使用静态硬编码的 `SECTOR_STOCKS`，改为调用 a-stock-data 的百度概念板块归属端点动态获取。

```typescript
// 动态获取板块成分股（调用 a-stock-data 端点）
async function getSectorStocks(sectorName: string): Promise<string[]> {
  // 调用 a-stock-data 的 baidu_concept_blocks() 端点
  // 或同花顺热点接口 ths_hot_reason()
  // 返回该板块下的所有股票代码列表
}

// 缓存机制：成分股每季度调整一次，缓存 7 天
const sectorStockCache: Record<string, { stocks: string[]; updatedAt: number }> = {};

async function getSectorStocksWithCache(sectorName: string): Promise<string[]> {
  const cached = sectorStockCache[sectorName];
  if (cached && Date.now() - cached.updatedAt < 7 * 24 * 3600 * 1000) {
    return cached.stocks;
  }
  const stocks = await getSectorStocks(sectorName);
  sectorStockCache[sectorName] = { stocks, updatedAt: Date.now() };
  return stocks;
}
```

### 5.2 预匹配策略

> **修正说明（v3→v4）**：恢复预匹配的"未匹配提示"，但调整措辞为"请综合判断传导链关联"，而不是"请特别严格审核"。

```typescript
interface PreMatchResult {
  matched: boolean;
  matchedTerms: string[];
  matchType: 'exact' | 'code' | 'sector' | 'fuzzy';
}

function preMatchFinancial(text: string, expandedKeywords: string[], keywordType: KeywordType): PreMatchResult {
  const lowerText = text.toLowerCase();
  const matchedTerms: string[] = [];
  
  for (const kw of expandedKeywords) {
    if (lowerText.includes(kw.toLowerCase())) {
      matchedTerms.push(kw);
    }
  }
  
  // 判断匹配类型
  let matchType: PreMatchResult['matchType'] = 'fuzzy';
  if (keywordType === 'stock_code' && matchedTerms.some(t => /^\d{6}$|^[A-Z]{1,5}$/.test(t))) {
    matchType = 'code';
  } else if (keywordType === 'sector' && matchedTerms.some(t => SECTOR_STOCKS[t])) {
    matchType = 'sector';
  } else if (matchedTerms.includes(expandedKeywords[0])) {
    matchType = 'exact';
  }
  
  return {
    matched: matchedTerms.length > 0,
    matchedTerms,
    matchType
  };
}
```

### 5.3 关键词子集关系检测（新增）

> **新增设计（v4）**：避免用户同时监控"宁德时代"和"新能源板块"时，宁德时代公告触发两次分析。

```typescript
// 关键词子集关系映射
const KEYWORD_SUBSETS: Record<string, string[]> = {
  '宁德时代': ['新能源', '新能源板块', '锂电池', '创业板'],
  '比亚迪': ['新能源', '新能源板块', '锂电池', '汽车'],
  '贵州茅台': ['白酒', '白酒板块', '消费'],
  // ... 动态维护
};

function findSupersetKeywords(keyword: string, allKeywords: string[]): string[] {
  // 返回包含当前关键词作为子集的所有关键词
  const supersets: string[] = [];
  for (const [subset, supersetList] of Object.entries(KEYWORD_SUBSETS)) {
    if (subset === keyword) {
      supersets.push(...supersetList);
    }
  }
  // 检查 allKeywords 中是否有 superset
  return supersets.filter(s => allKeywords.includes(s));
}

// 使用场景：当内容匹配了"宁德时代"时，同时标记为"新能源板块"也已命中
// 避免对同一内容重复进行 AI 分析
```

---

## 六、第3层：AI 智能分析层（核心引擎）

### 6.1 AI 分析维度设计

| 维度 | 类型 | 说明 | 是否借鉴 yupi-hot-monitor |
|------|------|------|--------------------------|
| `eventType` | string | 事件类型（见 6.2） | ❌ 新增 |
| `isSubstantial` | boolean | 是否有实质信息（过滤例行/历史回顾） | ✅ 替换 isReal |
| `relevance` | 0-100 | 与关键词的相关性 | ✅ 借鉴 |
| `relevanceReason` | string | 相关性打分理由 | ✅ 借鉴 |
| `keywordMentioned` | boolean | 是否直接提及关键词 | ✅ 借鉴 |
| `importance` | low/medium/high | 重要性分级 | ✅ 借鉴，但分级逻辑重写 |
| `importanceReason` | string | 重要性分级理由 | ❌ 新增 |
| `summary` | string | 核心事实摘要（一句话中文） | ✅ 借鉴，格式调整 |
| `affectedHoldings` | boolean | 是否影响持仓决策 | ❌ 新增 |
| `eventFingerprint` | string | 事件指纹（用于跨源去重） | ❌ 新增 |

### 6.2 事件类型识别（Event Classification）

金融信息的价值首先取决于"这是什么类型的事件"。同一公司，"高管变更"和"董事会决议"的重要性完全不同。

#### 6.2.1 事件类型体系

```typescript
type EventType = 
  // 公司事件
  | 'earnings'           // 财报/业绩预告
  | 'earnings_guidance'  // 业绩指引调整
  | 'executive_change'   // 高管变更
  | 'ma'                 // 并购/重组
  | 'equity_change'      // 股权变动/增持减持
  | 'financing'          // 定增/配股/发债
  | 'dividend'           // 分红/送转
  | 'lawsuit'            // 诉讼/仲裁
  | 'regulatory'         // 监管处罚/问询
  | 'product'            // 产品/技术突破
  | 'contract'           // 重大合同
  | 'shareholder_meeting'// 股东大会/董事会
  | 'routine'            // 例行公告
  // 宏观事件
  | 'macro_data'         // 宏观数据发布
  | 'monetary_policy'    // 货币政策（加息/降息/降准）
  | 'fiscal_policy'      // 财政政策
  | 'trade_policy'       // 贸易政策
  // 市场事件
  | 'market_movement'    // 市场异动
  | 'analyst_rating'     // 研报/评级调整
  | 'institutional'      // 机构持仓变化（13F等）
  // 其他
  | 'industry_policy'    // 行业政策
  | 'other';
```

#### 6.2.2 事件类型 → 基础重要性映射

不同事件类型有"基础重要性"，这是由金融常识决定的：

| 事件类型 | 基础重要性 | 说明 |
|----------|-----------|------|
| `ma` | high | 并购重组往往改变公司基本面 |
| `executive_change` | high | 核心高管变更是重大信号 |
| `regulatory` | high | 监管处罚/问询可能改变公司前景 |
| `earnings_guidance` | high | 业绩指引调整直接影响估值 |
| `monetary_policy` | high | 货币政策影响全局资产定价 |
| `equity_change` | medium-high | 大股东变动是重要信号 |
| `financing` | medium | 再融资影响股本和资金用途 |
| `earnings` | medium | 定期财报重要但可预期 |
| `macro_data` | medium-high | 宏观数据影响整个市场 |
| `dividend` | medium | 分红变化影响持有回报 |
| `lawsuit` | medium-high | 诉讼金额可能影响利润 |
| `product` | medium | 产品突破可影响竞争力 |
| `contract` | medium-high | 重大合同影响营收 |
| `institutional` | medium | 13F 持仓变化的信号价值因机构而异 |
| `industry_policy` | medium | 行业政策影响板块整体 |
| `trade_policy` | medium-high | 贸易政策影响进出口型企业 |
| `market_movement` | low-medium | 每天都有涨跌，需结合幅度判断 |
| `shareholder_meeting` | low | 例行通知 |
| `routine` | low | 例行公告 |
| `other` | 由 AI 判断 | — |

**注意**：基础重要性不是最终重要性，最终重要性还要结合"影响程度"判断。比如"CEO 辞职"（high）vs "独立董事辞职"（medium）。

#### 6.2.3 AI Prompt 设计

> **修正说明（v3→v4）**：
> 1. 恢复预匹配的"未匹配提示"，但调整措辞
> 2. 增加定期财报的"差异分析"提示
> 3. 增加宏观数据"预期 vs 实际"对比提示
> 4. 增加事件指纹输出

```typescript
function buildFinancialAnalysisPrompt(
  keyword: string,
  keywordType: KeywordType,
  preMatchResult: PreMatchResult,
  sourceType: 'announcement' | 'news' | 'macro_data',
  context?: { expectedValue?: number; previousValue?: number }  // 宏观数据专用
): string {
  const matchHint = preMatchResult.matched 
    ? `内容预匹配发现以下关键词变体：${preMatchResult.matchedTerms.join('、')}` 
    : `内容预匹配未直接提及关键词"${keyword}"的任何变体。注意：在宏观分析和板块研究中，关键词可能不直接出现但存在传导链关联（如美联储加息影响所有资产），请综合判断。`;

  const macroHint = sourceType === 'macro_data' && context
    ? `\n宏观数据对比：前值 ${context.previousValue}，预期值 ${context.expectedValue}，请判断实际值与预期的差异是否显著。`
    : '';

  const filingHint = sourceType === 'announcement' 
    ? `\n公告分析提示：如果这是定期财报（10-K/10-Q/年报/季报），请重点关注与上一期披露的差异部分（如业绩同比变化、风险因素变更、分部重组、商誉减值等），而不是重复已知信息。如果这是 8-K/A（修正案），请判断此修正案与原始 8-K 的差异是否实质性。`
    : '';

  return `你是金融信息分析专家。请分析以下${sourceType === 'announcement' ? '公告' : sourceType === 'macro_data' ? '宏观数据' : '财经资讯'}内容与监控关键词「${keyword}」的相关性和重要性。

${matchHint}${macroHint}${filingHint}

## 分析要求

### 1. 事件类型识别（eventType）
请判断这属于什么类型的事件，必须从以下列表中选择：
- 公司事件：earnings（财报）、earnings_guidance（业绩指引）、executive_change（高管变更）、ma（并购重组）、equity_change（股权变动）、financing（再融资）、dividend（分红）、lawsuit（诉讼）、regulatory（监管处罚）、product（产品突破）、contract（重大合同）、shareholder_meeting（股东大会）、routine（例行公告）
- 宏观事件：macro_data（宏观数据）、monetary_policy（货币政策）、fiscal_policy（财政政策）、trade_policy（贸易政策）
- 市场事件：market_movement（市场异动）、analyst_rating（研报评级）、institutional（机构持仓）
- 其他：industry_policy（行业政策）、other

### 2. 实质判断（isSubstantial）
判断内容是否包含实质信息：
- true：有实质性内容（如具体数字、具体决策、具体影响）
- false：例行公告、历史回顾、无实质内容的泛泛而谈
特别说明：定期财报（年报/季报/10-K/10-Q）默认为实质，除非内容为「预约披露时间提醒」等纯提示性信息。

### 3. 相关性评分（relevance，0-100）
- 90-100：直接且核心相关（如公司本身的重大公告）
- 70-89：直接相关（如行业政策影响该板块）
- 50-69：间接相关（如宏观经济影响整个市场，该公司也受影响）
- 30-49：弱相关（同一产业链但影响很小）
- 0-29：几乎无关

特别说明：
- 如果关键词是板块名（如"新能源"），板块内公司的重大事件应给 80+ 分
- 如果关键词是宏观指标（如"CPI"），CPI 数据发布本身应给 95+ 分
- 如果内容只是"同一领域但未提及关键词"， relevance 应低于 40 分

### 4. 关键词提及（keywordMentioned）
内容中是否直接提及了关键词或其等价表述（如股票代码对应的公司名）。

### 5. 重要性分级（importance）
综合事件类型和影响程度判断：
- high：明显影响持仓决策（如并购、高管变更、业绩暴雷、监管处罚、货币政策变化）
- medium：需要关注但不一定立即行动（如定期财报、行业政策、再融资）
- low：了解即可（如例行公告、历史回顾、无关痛痒的小新闻）

判断依据：
- 事件类型的基础重要性（并购 > 财报 > 例行公告）
- 影响程度（涉及金额大小、影响范围、是否可逆）
- 突发性（突发事件 > 可预期事件）

### 6. 核心事实摘要（summary）
用一句话中文说明"发生了什么"，要求：
- 包含关键数字（如有）
- 说明事件对关键词实体的直接影响
- 不超过 50 字

### 7. 是否影响持仓（affectedHoldings）
判断此事件是否可能导致投资者调整持仓（买入/卖出/加仓/减仓）。

### 8. 事件指纹（eventFingerprint）
生成一个简短的事件指纹，用于识别同一事件的多源报道。格式：公司名_事件类型_关键数字（如"宁德时代_投资_50亿"）。

## 输出格式
请以 JSON 格式输出，只输出 JSON，不要有其他内容：
{
  "eventType": "事件类型",
  "isSubstantial": true/false,
  "relevance": 0-100,
  "relevanceReason": "相关性打分理由",
  "keywordMentioned": true/false,
  "importance": "low/medium/high",
  "importanceReason": "重要性分级理由",
  "summary": "核心事实摘要",
  "affectedHoldings": true/false,
  "eventFingerprint": "事件指纹"
}`;
}
```

### 6.3 模型选择

| 阶段 | 模型 | 原因 |
|------|------|------|
| Query Expansion | DeepSeek-V4-Flash | 简单任务，低成本 |
| 内容分析 | DeepSeek-V4-Flash | 金融分析不需要最强模型，Flash 足够且成本低 |

**temperature**：0.2（降低随机性，提高判断一致性）

**maxTokens**：800（金融分析输出较长，需要更多 token）

---

## 七、第4层：阈值过滤层

### 7.1 过滤规则

```typescript
function shouldFilter(analysis: FinancialAIAnalysis): { pass: boolean; reason: string } {
  // 规则1：非实质内容直接过滤
  if (!analysis.isSubstantial) {
    return { pass: false, reason: '非实质内容（例行公告/历史回顾）' };
  }

  // 规则2：相关性低于 40 过滤
  // 金融领域信源质量高，阈值可从 50 降到 40
  if (analysis.relevance < 40) {
    return { pass: false, reason: `相关性过低 (${analysis.relevance} < 40)` };
  }

  // 规则3：关键词未提及且相关性不足 60 过滤
  if (!analysis.keywordMentioned && analysis.relevance < 60) {
    return { pass: false, reason: `关键词未提及且相关性不足 (${analysis.relevance} < 60)` };
  }

  // 规则4（可选）：low 重要性 + 快讯源 → 过滤（防止快讯刷屏）
  if (analysis.importance === 'low' && analysis.sourceType === 'news') {
    return { pass: false, reason: '低重要性快讯，不推送' };
  }

  return { pass: true, reason: '通过所有过滤规则' };
}
```

### 7.2 阈值对比

| 规则 | yupi-hot-monitor（通用） | 本项目（金融） | 调整原因 |
|------|------------------------|---------------|----------|
| relevance 最低分 | 50 | 40 | 金融信源质量高，可放宽 |
| keywordMentioned + relevance | < 65 过滤 | < 60 过滤 | 金融领域传导链分析可能不直接提及关键词 |
| isReal | 过滤假新闻 | 替换为 isSubstantial | 机构信源无假新闻，但需过滤例行内容 |
| low + 快讯 | 保留 | 过滤 | 防止快讯源刷屏 |

---

## 八、第5层：跨源去重与聚合层

### 8.1 问题定义

同一事件，多个信源可能报道：
- 巨潮公告："宁德时代关于投资建设新能源项目的公告"
- 财联社快讯："宁德时代拟投资 50 亿元建设新产能基地"
- 东财资讯："宁德时代大手笔扩产，新能源赛道再升温"

URL 不同，标题不同，但事件相同。需要识别并合并。

### 8.2 去重策略

#### 8.2.1 事件指纹匹配（AI 生成）

> **修正说明（v2→v4）**：不再使用粗糙的"标题去符号取前 30 字符"，改为使用 AI 生成的 `eventFingerprint` 字段。

```typescript
// AI 在分析时已经输出了 eventFingerprint
// 例如："宁德时代_投资_50亿"

function matchEventFingerprint(fp1: string, fp2: string): boolean {
  // 简单的字符串匹配即可，因为 AI 已经提取了核心要素
  return fp1 === fp2;
}
```

#### 8.2.2 时间窗口聚合

```typescript
// 同一事件的时间窗口：30 分钟
const EVENT_TIME_WINDOW_MS = 30 * 60 * 1000;

// 维护最近处理的事件指纹
const recentEvents: Array<{
  fingerprint: string;
  timestamp: number;
  sources: string[];
  primarySource: string;
}> = [];
```

#### 8.2.3 同一公司多公告批处理（新增）

> **新增设计（v4）**：同一公司 30 分钟内的多条公告，合并为一条"综合公告"推送。

```typescript
interface MergedAnnouncement {
  companyName: string;
  announcements: RawContent[];
  timestamp: number;
  combinedFingerprint: string;
}

function shouldMergeAnnouncements(
  newItem: RawContent,
  pendingItems: RawContent[]
): boolean {
  // 检查是否有同一公司 30 分钟内的待处理公告
  const sameCompany = pendingItems.filter(item => 
    item.companyName === newItem.companyName &&
    Math.abs(item.timestamp - newItem.timestamp) < 30 * 60 * 1000
  );
  return sameCompany.length > 0;
}

// 合并后的 AI 分析：一次性分析多条公告，输出综合摘要
// 例如："宁德时代今日发布 3 条公告：① 投资 50 亿建设新产能 ② 高管变更 ③ 一季度业绩预告"
```

#### 8.2.4 主源选择策略

当同一事件被多个信源报道时，选择"最权威"的版本作为主记录：

| 优先级 | 信源类型 | 原因 |
|--------|----------|------|
| 1 | SEC EDGAR / 巨潮公告 | 原始披露，最权威 |
| 2 | 财联社快讯 | 记者采编，有解读 |
| 3 | 东财资讯 | 综合资讯 |
| 4 | FRED / NBS | 宏观数据 |

```typescript
const SOURCE_AUTHORITY: Record<string, number> = {
  'sec_edgar': 1,
  'juchao': 2,
  'cailianshe': 3,
  'eastmoney': 4,
  'fred': 5,
  'nbs': 6
};

function selectPrimarySource(sources: string[]): string {
  return sources.sort((a, b) => 
    (SOURCE_AUTHORITY[a] || 99) - (SOURCE_AUTHORITY[b] || 99)
  )[0];
}
```

#### 8.2.5 合并展示

前端展示时，主记录显示完整信息，同时标注"该事件还被 X 个信源报道"。

---

## 九、第6层：推送决策层

### 9.1 推送规则

| importance | 推送方式 | 前端标记 |
|-----------|----------|----------|
| high | 立即 WebSocket 推送 + 声音/视觉提醒 | 🔴 高 |
| medium | 立即 WebSocket 推送 | 🟡 中 |
| low | 不推送，仅入库 | 🟢 低（用户主动查看时可见） |

### 9.2 防重复打扰

```typescript
// 同一事件 30 分钟内只推送一次
const pushCooldownMs = 30 * 60 * 1000;
const lastPushTimes: Record<string, number> = {};

function shouldPush(eventFingerprint: string): boolean {
  const lastPush = lastPushTimes[eventFingerprint];
  if (!lastPush || Date.now() - lastPush > pushCooldownMs) {
    lastPushTimes[eventFingerprint] = Date.now();
    return true;
  }
  return false;
}
```

### 9.3 推送内容格式

```typescript
interface PushNotification {
  type: 'hotspot';
  title: string;           // 标题（如"宁德时代：拟投资50亿元建设新产能"）
  content: string;         // AI 摘要
  importance: 'high' | 'medium' | 'low';
  source: string;          // 主信源
  relatedSources: string[];// 相关信源列表
  url: string;             // 原始链接
  eventType: string;       // 事件类型
  relevance: number;       // 相关性分数
  timestamp: number;       // 发布时间
}
```

---

## 十、数据模型

### 10.1 数据库表设计

```prisma
model Keyword {
  id        String   @id @default(uuid())
  text      String   // 用户输入的关键词
  type      String   // stock_code / stock_name / sector / macro / policy / generic
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  hotspots  Hotspot[]
}

model Hotspot {
  id                String   @id @default(uuid())
  title             String
  content           String   @db.Text
  url               String
  source            String   // sec_edgar / juchao / cailianshe / eastmoney / fred / nbs
  sourceType        String   // announcement / news / macro_data
  
  // AI 分析结果
  eventType         String   // 事件类型
  isSubstantial     Boolean
  relevance         Int      // 0-100
  relevanceReason   String?  @db.Text
  keywordMentioned  Boolean
  importance        String   // low / medium / high
  importanceReason  String?  @db.Text
  summary           String?  @db.Text
  affectedHoldings  Boolean
  eventFingerprint  String?  // 事件指纹，用于跨源去重
  
  // 去重与聚合
  relatedSources    String?  // JSON 数组，相关信源列表
  isPrimary         Boolean  @default(true) // 是否为主记录
  isMerged          Boolean  @default(false) // 是否为合并公告
  mergedItems       String?  // JSON 数组，合并的子公告列表
  
  // 时间
  publishedAt       DateTime?
  createdAt         DateTime @default(now())
  
  // 关联
  keywordId         String
  keyword           Keyword  @relation(fields: [keywordId], references: [id])
  notifications     Notification[]
}

model Notification {
  id        String   @id @default(uuid())
  type      String   @default('hotspot')
  title     String
  content   String?
  isRead    Boolean  @default(false)
  createdAt DateTime @default(now())
  
  hotspotId String
  hotspot   Hotspot  @relation(fields: [hotspotId], references: [id])
}

model SourceWatermark {
  id        String   @id @default(uuid())
  source    String   @unique
  lastId    String?  // 最后处理的 ID
  lastTimestamp Int? // 最后处理的时间戳
  updatedAt DateTime @updatedAt
}

model MacroObservation {
  id        String   @id @default(uuid())
  seriesId  String   @unique // FRED Series ID 或 NBS 指标代码
  lastDate  String   // 最新 observation 的 date
  lastValue Float?   // 最新值
  expectedValue Float? // 市场预期值（如可获得）
  updatedAt DateTime @updatedAt
}
```

---

## 十一、与 v2/v3 的变更对照

### v2 → v4 的修正

| 问题 | v2 方案 | v4 修正 |
|------|---------|---------|
| MVP 优先级 | A 股信源放 P1 | 6 个信源全部进 P0 |
| 10-K/10-Q 分类 | 列为例行公告（P2） | 提升为 P1，正常进 AI，Prompt 提示关注差异 |
| 8-K/A 分类 | 列为例行公告 | 移除黑名单，正常进 AI |
| 成分股映射 | 静态硬编码 | 调用 a-stock-data 动态获取 + 7 天缓存 |
| 事件指纹 | 标题去符号取前 30 字符 | AI 直接生成 eventFingerprint |

### v3 → v4 的修正

| 问题 | v3 方案 | v4 修正 |
|------|---------|---------|
| 预匹配逻辑 | 弱化"未匹配提示" | 恢复提示，但措辞改为"请综合判断传导链关联" |
| 预约披露过滤 | 交给 AI 判断 | 采集层直接过滤，不进 AI |
| 快讯配额排序 | 先到先得 | 按事件类型优先级排序 |
| 事件指纹 | 仍无可靠实现 | AI 直接输出 eventFingerprint |
| 宏观数据预期 | 未提及 | Prompt 增加"预期 vs 实际"对比 |
| 关键词子集 | 未提及 | 新增子集关系检测，避免重复分析 |
| 同一公司多公告 | 未提及 | 新增 30 分钟合并策略 |

---

## 十二、与 yupi-hot-monitor 的借鉴关系总结

| yupi-hot-monitor 机制 | 借鉴方式 | 调整内容 |
|----------------------|----------|----------|
| Query Expansion | ✅ 借鉴框架 | 扩展策略从"文本变体"改为"金融实体关联" |
| 预匹配 | ✅ 借鉴框架 | 增加 matchType（exact/code/sector/fuzzy），调整未匹配提示措辞 |
| isReal | ✂️ 替换为 isSubstantial | 机构信源无假新闻，但需过滤例行内容 |
| relevance 0-100 | ✅ 借鉴框架 | 评分标准重写，增加传导链感知 |
| keywordMentioned | ✅ 直接借鉴 | 无调整 |
| importance | ✅ 借鉴框架 | 分级逻辑从"社交传播力"改为"事件类型 × 影响程度" |
| summary | ✅ 借鉴框架 | 输出格式从"与关键词的关系"改为"核心事实摘要" |
| relevanceReason | ✅ 直接借鉴 | 增加 importanceReason |
| 三层阈值过滤 | ✅ 借鉴框架 | 阈值调整（50→40, 65→60） |
| 批量并发控制 | ✅ 直接借鉴 | 无调整 |
| 去重 | ✅ 借鉴框架 | URL 去重 → AI 生成的事件指纹去重 |
| 新鲜度过滤 | ✂️ 改造 | 7 天统一窗口 → 按信源类型分层（快讯小时级/公告天级/宏观数据更新检测） |
| 来源优先级 | ✅ 借鉴框架 | 从"实时性强弱"改为"权威层级" |
| 配额控制 | ✅ 借鉴框架 | 从"Twitter 15 + 其他 10"改为"每源 5，总额 30"，快讯按事件类型排序 |
| 热度评分公式 | ❌ 不借鉴 | 金融场景不适用互动数据 |

---

## 十三、MVP 实现范围

### P0（MVP 本期）—— 6 个信源完整链路

1. **信源采集层**：SEC EDGAR + FRED + NBS + 巨潮 + 财联社 + 东财
2. **信源内过滤**：
   - 纯提示性公告过滤（预约披露时间等）
   - 例行公告过滤（董事会决议等）
   - 快讯时间戳去重
   - 宏观数据更新检测
3. **关键词匹配层**：
   - 金融实体 Query Expansion
   - 预匹配
   - 关键词子集关系检测
4. **AI 分析层**：事件类型识别 + 相关性 + 重要性 + 摘要 + 事件指纹
5. **阈值过滤层**：三层过滤规则
6. **跨源去重层**：事件指纹匹配 + 时间窗口聚合 + 主源选择
7. **推送决策层**：WebSocket 实时推送 + 防重复打扰

### P1（后续迭代）

- 搜索功能（复用 a-stock-data 端点做主动查询）
- 市场验证层（研报/龙虎榜/北向/融资融券）
- 同一公司多公告批处理合并
- 事件指纹算法优化（如 AI 指纹不够准确，可补充 NLP 提取）

### P2

- 关键词类型自动识别（替代当前的规则匹配）
- 金融传导链自动分析（上下游关联）
- 用户自定义推送规则
- 宏观数据预期值自动获取

---

> **文档状态**：最终版，待确认
> **下一步**：用户确认后，输出 TECH_DESIGN.md 开始技术实现
