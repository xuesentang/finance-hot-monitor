# 金融热点监控工具 — MVP 需求文档 v2.1

> 本文档定义 MVP 第一期要做的事。v1.0 因信息筛选机制过于粗糙已废弃，v2.0 整合了完整的信息筛选设计和所有已确认决策，v2.1 反映搜索功能已阶段性放弃的实际状态。
> 完整产品愿景见 [PRD v1.0](./PRD-金融热点监控工具-v1.0.md)，信息筛选详见 [信息筛选机制设计（4）](./信息筛选机制设计（4）.md)。

---

## 一、MVP 核心定义

**一句话**：替用户盯着 A 股和美股市场的公告、快讯和宏观数据，有动静了自动推送，不用自己盯盘。

**核心链路**：采集 → AI 分析 → 通知

**与完整 PRD 的关系**：完整 PRD 是地图，MVP 是第一站。完整 PRD 有四层漏斗（S/A/B/C）、搜索功能、市场验证层；MVP 只做事件检测层的监控链路。搜索功能曾实现但因效果不佳已阶段性放弃，前端入口已封禁隔离。

---

## 二、MVP 信源（6 个）

| 信源 | 市场 | 内容 | 信源类型 | 接入方式 | 轮询频率 |
|------|------|------|----------|---------|---------|
| SEC EDGAR | 美股公告 | 10-K/10-Q/8-K/13F 等法定披露 | 公告类 | 官方 API，免费，10 req/s | 每 10 分钟 |
| FRED | 美国宏观 | CPI/GDP/利率/非农/就业 | 宏观类 | 官方 API，免费需 Key，60 次/时 | 每 1 小时 |
| 国家统计局 (NBS) | 中国宏观 | GDP/CPI/PPI/PMI/工业增加值 | 宏观类 | `data.stats.gov.cn/easyquery.htm` 公开 API | 每 1 小时 |
| 巨潮资讯 | A 股公告 | 上市公司法定披露公告全文 | 公告类 | cninfo.com.cn 直连 HTTP | 每 10 分钟 |
| 财联社 | A 股快讯 | 电报式实时快讯，记者采编 | 快讯类 | cls.cn 直连 HTTP | 每 2 分钟 |
| 东财全球资讯 | A 股快讯 | 7×24 滚动财经快讯 | 快讯类 | np-weblist.eastmoney.com 直连 HTTP | 每 2 分钟 |

全部为真实公开 API 或直连 HTTP 端点，不涉及脆弱爬虫层。A 股信源接入代码参考 [a-stock-data](https://github.com/simonlin1212/a-stock-data) Skill。

---

## 三、信息筛选机制（六层漏斗）

详见 [信息筛选机制设计（4）](./信息筛选机制设计（4）.md)，此处概述。

```
第0层：信源采集层 — 按信源类型（公告/快讯/宏观）定制采集策略
第1层：信源内过滤 — 例行公告过滤 + 时间戳去重 + 宏观数据变更检测
第2层：关键词匹配 — 金融实体 Query Expansion + 预匹配 + 关键词子集检测
第3层：AI 智能分析 — 事件分类 → 相关性 → 重要性 → 摘要 → 事件指纹
第4层：阈值过滤 — isSubstantial + relevance ≥ 40 + keywordMentioned/relevance 联合判断
第5层：跨源去重聚合 — 事件指纹匹配 + 30 分钟时间窗口 + 主源选择
第6层：推送决策 — high/medium 即时推送，low 仅入库
```

### 3.1 第0层：信源采集层

按信源类型分三种采集模式：

- **公告类**（SEC EDGAR / 巨潮）：轮询新文件列表，按公告类型分优先级（P0 资产重组/股权变动/高管变更 > P1 业绩预告/定期财报 > P3 例行决议）
- **快讯类**（财联社 / 东财）：高频轮询，水位线去重，只处理增量
- **宏观类**（FRED / NBS）：关键词映射到指标 ID，检测最新 observation date 是否变更，变更才触发

### 3.2 第1层：信源内过滤

- **公告类**：过滤纯提示性公告（预约披露时间）+ 例行公告（董事会决议/股东大会通知/监事会决议/独立董事意见/DEFA14A/SUPPL）。10-K/10-Q/8-K/A 不过滤，正常进 AI。
- **快讯类**：时间戳/ID 去重
- **宏观类**：数据值未变化不触发

### 3.3 第2层：关键词匹配

**Query Expansion**：先识别关键词类型（股票代码/公司名/板块/宏观指标/政策/通用），再按类型走不同扩展策略。如「300750」→ 扩展为「300750, 宁德时代, CATL, 宁德」；「新能源」→ 扩展为「新能源, 300750, 002594, 锂电, 光伏」。板块成分股通过 a-stock-data 的百度概念板块端点动态获取。

**预匹配**：扩展后的关键词列表在文本中做子串匹配，结果传给 AI 作为判断提示。

**关键词子集检测**：避免「宁德时代」和「新能源板块」同时监控时，同一条内容触发两次 AI 分析。

### 3.4 第3层：AI 智能分析（核心引擎）

调用 **DeepSeek-V4-Flash**（非 OpenRouter），temperature 0.2。

| 维度 | 类型 | 说明 |
|------|------|------|
| `eventType` | 20+ 事件类型枚举 | 先识别事件类型，再映射基础重要性 |
| `isSubstantial` | boolean | 是否有实质信息（替换 isReal，机构信源无假新闻） |
| `relevance` | 0-100 | 含传导链感知。90+ 核心相关，70-89 直接相关，50-69 间接相关，<40 过滤 |
| `relevanceReason` | string | 打分理由，便于调试 |
| `keywordMentioned` | boolean | 是否直接提及关键词 |
| `importance` | low/medium/high | 事件类型 × 影响程度双因子判断 |
| `importanceReason` | string | 分级理由 |
| `summary` | string | 一句话中文核心事实摘要，≤50 字 |
| `affectedHoldings` | boolean | 是否可能影响持仓决策 |
| `eventFingerprint` | string | AI 生成的事件指纹，用于跨源去重 |

**重要性分级规则**：

| 级别 | 含义 | 典型事件 |
|------|------|---------|
| high | 明显影响持仓决策 | 并购重组、CEO 变更、监管处罚、业绩暴雷、货币政策重大转向 |
| medium | 需关注但不一定立即行动 | 定期财报、行业政策、再融资、一般性评级调整 |
| low | 了解即可，不推送 | 例行公告、小幅度股权变动、历史回顾、无关快讯 |

### 3.5 第4层：阈值过滤

| 规则 | 条件 | 结果 |
|------|------|------|
| 规则1 | `!isSubstantial` | 丢弃 |
| 规则2 | `relevance < 40` | 丢弃 |
| 规则3 | `!keywordMentioned && relevance < 60` | 丢弃 |
| 规则4 | `importance === 'low' && 信源为快讯类` | 丢弃（防刷屏） |

### 3.6 第5层：跨源去重与聚合

同一事件被多源报道（如巨潮发公告 + 财联社报道 + 东财转载），通过 AI 生成的 `eventFingerprint`（格式：`公司名_事件类型_关键数字`）匹配 + 30 分钟时间窗口，识别为同一事件。保留最权威信源的版本作为主记录，前端标注「同时被 X 个信源报道」。

权威排序：SEC EDGAR / 巨潮 > 财联社 > 东财 > FRED / NBS

### 3.7 第6层：推送决策

| importance | 动作 |
|-----------|------|
| high | 立即 WebSocket 推送 |
| medium | 立即推送 |
| low | 不推送，仅入库（用户可在热点列表中按级别筛选查看） |

同一事件指纹 30 分钟内只推送一次。每关键词每轮次：每个信源最多 5 条进入 AI，总额 30 条。

---

## 四、MVP 功能清单

### 4.1 关键词管理

- 添加/删除关键词
- 关键词列表展示
- 启用/暂停单个关键词

**不做**：关键词类型手动分类、AND/OR 组合逻辑

### 4.2 热点列表

- 时间倒序展示捕获的热点
- 按重要性筛选（high/medium/low）
- 按信源筛选
- 查看详情（原始内容 + AI 分析结果 + 来源链接 + 关联信源）

**不做**：已读标记

### 4.3 通知

- WebSocket 实时推送到前端页面
- 防重复推送（事件指纹 + 30 分钟冷却）

**不做**：邮件通知、通知规则配置、每日汇总

### 4.4 前端页面

| 页面 | 功能 |
|------|------|
| 关键词管理 | 增删关键词，启用/暂停，查看列表 |
| 热点列表 | 时间倒序，按重要性/信源筛选，查看详情 |

**不做**：仪表盘、设置页、健康监控面板

---

## 五、MVP 明确不做

| 不做 | 原因 | 计划 |
|------|------|------|
| **搜索功能**（主动查询某公司财报/高管变更等） | 已实现但效果不佳，已阶段性放弃并隔离封禁。前端入口（路由/import/API/类型）全部注释，后端路由已注释，代码保留在仓库中 | 待重新设计搜索方案后恢复，或彻底删除 |
| **市场验证层**（研报/龙虎榜/北向/融资融券） | 属于「市场怎么反应」的验证层 | 监控功能稳定后再做 |
| 央行（PBOC）政策数据 | 无统一 API，数据碎片化 | 后续评估 |
| 邮件通知 | 增加 SMTP 部署复杂度 | V1.0 |
| 通知规则配置 | 用户量=1，默认推送 medium 以上即可 | V1.0 |
| 仪表盘 / 设置页 | 运营视图，此时用不上 | V1.5 |
| 数据源健康监控 / 自动降级 | MVP 信源数量少，手动监控 | V1.5 |
| 同一公司多公告批处理合并 | 过度设计，不相关公告硬拼降低清晰度 | 后续评估 |
| 宏观数据预期值（Bloomberg 共识） | 数据源不可得 | 后续评估 |

---

## 六、数据模型

```
Keyword
  id, text, type, isActive, createdAt

Hotspot
  id, title, content, url, source, sourceType
  — AI 分析 —
  eventType, isSubstantial, relevance, relevanceReason
  keywordMentioned, importance, importanceReason
  summary, affectedHoldings, eventFingerprint
  — 去重聚合 —
  relatedSources, isPrimary
  — 时间 —
  publishedAt, createdAt
  keywordId

Notification
  id, type, title, content, isRead, createdAt, hotspotId

SourceWatermark
  id, source (@unique), lastId, lastTimestamp, updatedAt

MacroObservation
  id, seriesId (@unique), lastDate, lastValue, updatedAt
```

仅 5 张表（其中 SourceWatermark 和 MacroObservation 是支撑筛选逻辑的元数据表）。

---

## 七、MVP 验收标准

| 标准 | 目标 |
|------|------|
| 6 个信源均能成功拉取数据 | 手动运行 1 小时无报错 |
| AI 筛选有效 | 人工抽查 20 条推送，相关性判断准确率 > 70% |
| 前端可用 | 关键词 CRUD + 热点列表筛选查看，操作不报错 |
| 推送链路通畅 | 新热点从入库到前端通知 < 30 秒 |
| 跨源去重生效 | 同一事件多源报道不重复推送 |

---

## 八、技术栈（继承自 yupi-hot-monitor，已确认）

- 后端：Node.js + Express 5 + Prisma + SQLite + Socket.io
- AI：DeepSeek-V4-Flash API（替换 OpenRouter）
- 数据采集：Python 脚本（调用 a-stock-data 端点 + SEC EDGAR/FRED/NBS API）
- 前端：React 19 + TypeScript + Vite + Tailwind CSS

---

## 九、后续迭代路径

```
MVP（本期）
  └─ 监控链路：6 信源 → 六层筛选 → AI 分析 → WebSocket 推送
  └─ 搜索功能：已实现但因效果不佳已隔离封禁，待重新设计或删除

V1.0（下期）
  ├─ 搜索功能重新设计（或彻底移除搜索代码）
  └─ 邮件通知

V1.5
  ├─ 市场验证层：研报/龙虎榜/北向/融资融券
  └─ 数据源健康监控 + 仪表盘

V2.0
  └─ 传导链分析 + 情绪趋势 + 跨市场关联
```

---

> **文档状态**：已更新至 v2.1，反映搜索功能已阶段性放弃并隔离封禁的实际状态
> **下一步**：专注监控功能优化与稳定性验证。
